[package]
name = "tradar-core"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
directories = "6"
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
