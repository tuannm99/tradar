# Backlog

Theo dõi roadmap tính năng DB-IDE và các vấn đề đã biết bị gác lại từ các lần làm trước. Đây là bản ghi bền vững về trạng thái dự án và lý do thiết kế — `docs/architecture.md` bao quát thiết kế hệ thống, file này bao quát cái gì đã xong/đang làm/chưa bắt đầu và tại sao. Cập nhật trực tiếp file này mỗi khi một sub-project bắt đầu/kết thúc hoặc một issue mới bị gác lại.

## Roadmap sub-project

Tách ra từ một yêu cầu rộng hơn ("DataGrip/DBeaver-style DB IDE features: vim keymap, save/load, sessions, UI polish"). Mỗi mục dưới đây tự theo dõi thiết kế/trạng thái ngay trong dòng của nó thay vì qua các tài liệu spec/plan riêng.

1. **Migration sang Component Architecture** — xong, đã merge vào `master` (2026-08-02/03). Thay thiết kế `App`/`tui` phẳng bằng `src/action.rs` (enum `Action` + trait `Component`) + `src/components/`. Nền tảng cho các mục bên dưới. (Ghi chú 2026-08-08: các đường dẫn `src/...` trong mục này là lịch sử tại thời điểm merge; sau khi tách workspace, các file này nằm ở `crates/tradar-app/src/...` — xem `docs/architecture.md`.)
2. **Vim keybinding xuyên suốt TUI** — xong, đã merge. Phạm vi được sửa lại 2026-08-07: đây *không chỉ* là query editor. Yêu cầu thật sự là vim-style keybinding trên mọi panel — danh sách connection, query editor, và results — với panel results còn hỗ trợ thêm copy/yank dễ dàng một hoặc nhiều record. Chi tiết:
   - **Query editor** (dùng `edtui`) — xong, đã merge vào `master` (commit `1255e54`, gồm cả việc nâng `ratatui` 0.29 → `0.30` trong `Cargo.toml`). `edtui::EditorState` nằm dưới dạng field thuần trên `QueryEditorComponent` (do `QueryScreenComponent` sở hữu, là component host nó), không gắn cứng vào `Action`/`Component`; `Esc` phụ thuộc ngữ cảnh (Insert/Visual/Search→Normal mode trong editor trước, chỉ nổi lên thành `BackToPicker` khi đã ở Normal mode) — việc này đụng vào đúng thứ tự arm của `handle_key_event` mà cả schema-sidebar, migration Component, và review cuối của chính việc này đều phải giữ cho đúng; test hồi quy cho phần này nằm ở `components/query_screen.rs` (nay ở `crates/tradar-app/src/components/query_screen.rs`). Từng cân nhắc syntax highlighting cho SQL (edtui có sẵn feature `syntax-highlighting` dựa trên `syntect`) và chủ đích bỏ qua — dependency nặng, chưa được scope.
   - **Điều hướng danh sách connection / schema sidebar** — xong, đã merge. Cả `ConnectionPickerComponent` và `SchemaSidebarComponent` giờ hỗ trợ `j`/`k`/phím mũi tên, `gg`/`G` (nhảy lên đầu/xuống cuối), và `Ctrl+d`/`Ctrl+u` (cuộn nửa trang, tính theo số dòng hiển thị lần render gần nhất của mỗi component, theo dõi qua field `visible_height` set trong `draw()`). Nhận diện `gg` dùng field `pending_g: bool`, tiêu thụ qua `std::mem::take` ở đầu `handle_key_event`, nên bất kỳ phím nào khác sẽ huỷ một `g` đơn lẻ thay vì để nó bị kẹt. Với schema sidebar (bản thân không phải `Component`), các binding mới nối theo cùng cách với `j`/`k` hiện có: arm phím khi sidebar focus của `QueryScreenComponent` trả về các variant mới `Action::SchemaMoveTop`/`SchemaMoveBottom`/`SchemaHalfPageUp`/`SchemaHalfPageDown`, xử lý trong `update()`. Khi editor đang focus thay vào đó, `g`/`G` rơi thẳng xuống xử lý vim riêng của `edtui`, không đụng vào.
   - **Điều hướng vim + copy trên panel results** — xong, đã merge. `ResultsComponent` giờ theo dõi một index `selected: usize` (một row cho `QueryResult::Table`, một document cho `QueryResult::Documents`), render qua `List`/`ListState` (thay cho `Paragraph` phẳng cũ) với cùng điều hướng `j`/`k`/`gg`/`G`/`Ctrl+d`/`Ctrl+u` như schema sidebar. `Focus` có thêm variant thứ ba, `Results`; `Tab` giờ xoay vòng `Editor → Results → Sidebar → Editor`. Cơ chế copy: `y` yank row đang chọn (tách bằng tab) hoặc document (pretty-print JSON) qua escape sequence OSC52 (`Action::Yank { text }`, xử lý trong `main.rs` giống `ExportCurl` — không cần crate clipboard, hoạt động qua SSH/tmux trên terminal hỗ trợ OSC52). Đã verify từ đầu tới cuối trên terminal thật (tmux): connect, chạy một query nhiều row, `Tab` vào Results, `j`/`gg`/`G` di chuyển highlight đúng, `y` không crash.
3. **Lưu/mở query ra file** — xong, đã merge (2026-08-12). `QueryScreenComponent` có thêm `Ctrl+S` (lưu buffer hiện tại của `QueryEditorComponent` ra file) và `Ctrl+O` (đọc một file vào buffer, ghi đè nội dung hiện tại), hoạt động bất kể `focus` đang ở đâu (cùng pattern với `Ctrl+Y` export curl). Dựng mới một primitive UI trước đó chưa tồn tại trong app — `FilePromptComponent` (`crates/tradar-query-workbench/src/components/file_prompt.rs`), một ô nhập text một dòng (không phải `Component`, được `QueryScreenComponent` điều khiển trực tiếp giống `QueryEditorComponent`/`ResultsComponent`/`SchemaSidebarComponent`): `Enter` xác nhận, `Esc` huỷ, hỗ trợ điều hướng con trỏ (`Left`/`Right`/`Home`/`End`/`Backspace`/`Delete`). Khi prompt đang mở, `QueryScreenComponent::handle_key_event` chặn và định tuyến toàn bộ phím vào prompt trước tiên (ở đầu hàm, trước match chính) thay vì đi qua `Tab`/focus như bình thường. Lỗi IO (file không tồn tại khi mở, path rỗng, không ghi được...) hiện ngay trong prompt (viền đỏ) và giữ prompt mở thay vì đóng lại, để không mất input đã gõ. Đường dẫn lưu/mở gần nhất nhớ trong bộ nhớ phiên (`QueryScreenComponent.last_path`, không phải lưu ra config — không persist qua các lần chạy app) để prefill lần prompt tiếp theo. File IO chạy đồng bộ (`std::fs::write`/`std::fs::read_to_string`) ngay trong `tradar-query-workbench`, không phải trong driver nào — không vi phạm quy tắc cách ly vì đây không phải logic connector. Test: `crates/tradar-query-workbench/src/components/file_prompt.rs` (test riêng cho input widget) + `crates/tradar-query-workbench/src/components/query_screen.rs` (test tích hợp `Ctrl+S`/`Ctrl+O`, dùng `tempfile` — crate mới thêm `[dev-dependencies]`).
4. **Sessions/workspace state** — xong, đã merge (2026-08-12), cả 3 hướng user chọn khi scope (query history, nhiều tab/connection cùng lúc, state sống qua các lần chạy app). Làm theo thứ tự nhỏ → lớn, ba commit riêng:
   - **Query history** — xong. `QueryEngine` vốn đã lưu `history: Vec<String>` (không có UI đọc lại). Thêm `HistoryPickerComponent` (`crates/tradar-query-workbench/src/components/history_picker.rs`) — overlay list read-only, most-recent-first, điều hướng `j`/`k`/`gg`/`G`, `Enter` để load một entry vào editor (không tự chạy — an toàn hơn khi query có thể là DELETE/UPDATE), `Esc` huỷ. `Ctrl+R` mở, cùng cơ chế chặn-key-khi-overlay-mở với `FilePromptComponent` (sub-project 3).
   - **Nhiều tab/connection cùng lúc** — xong, phần lớn công sức nằm ở đây. Đây chính là `Workspace → Tab → Screen` từng bị gác lại trong `docs/architecture.md` (mục "Đã cân nhắc và gác lại"), triển khai thực dụng: không đổi tên `RootComponent`/không tạo type `Workspace` mới, chỉ thêm `pub struct Tab { screen: ScreenSlot, connection_picker: ConnectionPickerComponent, title: Option<String> }` và đổi `RootComponent` từ một `screen`/`connection_picker` sang `tabs: Vec<Tab>` + `active_tab: usize`. `Ctrl+T` tab mới, `Ctrl+W` đóng tab hiện tại (no-op nếu chỉ còn 1 tab), `Ctrl+Left`/`Ctrl+Right` chuyển tab (clamp, không wrap). Tab bar chỉ vẽ khi có ≥2 tab (1 tab thì layout y hệt trước khi có tính năng này, không tốn dòng nào). Thay đổi lan tới `Action` (đóng, ở `tradar-core`): `OpenRequested`/`Opened`/`OpenFailed` có thêm field `tab: usize`, mang xuyên suốt qua `main.rs`'s `spawn_connect`/`ConnectOutcome` để một reply connect luôn khớp đúng tab đã phát ra nó — kể cả khi user đã chuyển tab khác hoặc đóng hẳn tab gốc trong lúc đang connect (guard theo `epoch` của *tab đó*, không phải global; `tabs.get_mut(tab)` trả `None` an toàn nếu tab đã bị đóng). `ConnectionPickerComponent` không biết gì về tab (không phụ thuộc ngược) — nó set `tab: 0` placeholder, `RootComponent::handle_key_event` là nơi duy nhất ghi đè lại index thật. Verify tay qua tmux: mở 2 tab, connect tab 2 vào sqlite trong khi tab 1 vẫn ở picker, chạy query trên tab 2, chuyển qua lại — không tab nào đụng tab kia.
   - **State sống qua các lần chạy app** — xong. `tradar-core::storage` có thêm `SessionState { active_tab, tabs: Vec<String> }` (tên connection, theo thứ tự tab) + `SessionStore` (đọc/ghi `~/.config/tradar/session.toml`, cùng pattern với `ConnectionStore`). Chỉ tab đang `Active` (đã connect) mới được nhớ — tab còn ở picker thì không có gì để reconnect. Khởi động: `RootComponent::restore_tabs()` dựng lại từng tab bằng cách "giả lập" đúng key-press Enter trên picker của tab đó (tái dùng nguyên logic có sẵn thay vì thêm code mới) rồi trả về danh sách `Action::OpenRequested`, `main.rs` đưa thẳng vào `spawn_connect` y hệt một connect do user bấm tay. Tên connection không còn khớp trong `connections.toml` (đổi tên/xoá) thì bị bỏ qua lặng lẽ. **Phát hiện một lỗ hổng UX thật trong lúc verify tay**: trước đây không có cách quit khi đang ở active screen ngoài `Esc` (→ picker) rồi `q` — nhưng `Esc` xoá `title` của tab *trước khi* session kịp lưu, nên tab đang connect (trường hợp phổ biến nhất) không bao giờ được nhớ lại. Thêm `Ctrl+Q` (quit toàn cục, hoạt động ở bất kỳ đâu, chặn ở đầu `RootComponent::handle_key_event` cùng chỗ với `Ctrl+T`/`Ctrl+W`) để fix. **Chưa làm**: không restore lại nội dung query editor đang gõ dở (chỉ reconnect connection, editor luôn trống khi mở lại) — cần đổi `Session::build_screen` để nhận thêm text khởi tạo, việc đó đụng vào ranh giới `tradar-connector-api` mà chưa có nhu cầu cụ thể nào đòi hỏi ngay; để dành nếu sau này thấy cần. Verify tay qua tmux: connect → `Ctrl+Q` → mở lại `tradar` → tự reconnect vào đúng connection, không cần chọn lại từ picker.
5. **Đánh bóng UI tổng thể** — phần "dọn known issue nhỏ" đã xong (2026-08-12): 3 mục trong "Vấn đề đã biết" bên dưới đã fix (clear `last_error` khi retry, đồng nhất `Default`/`new()`, redraw chỉ khi có thay đổi thay vì vô điều kiện mỗi 50ms). User yêu cầu thêm 2 việc lớn (2026-08-12), scope qua `AskUserQuestion`:
   - **Dedupe key mapping vào 1 module dùng chung** — xong. Logic vim-movement (`j`/`k`/`gg`/`G`/`Ctrl-d`/`Ctrl-u`) bị copy-paste ở `ConnectionPickerComponent`, `SchemaSidebarComponent`, `ResultsComponent`, `HistoryPickerComponent`. Gom vào `tradar_core::vim_list` (module mới, `crates/tradar-core/src/vim_list.rs`) — API 2 hàm: `recognize(code, modifiers, &mut pending_g) -> Option<VimMove>` (phím → ý định) và `apply(mv, &mut selected, len, visible_height)` (ý định → mutate). Mỗi component giữ nguyên field/API public cũ (`selected`, `schema_selected`...), chỉ phần thân các hàm `move_*` đổi sang gọi module chung. `HistoryPickerComponent` được `Ctrl-d`/`Ctrl-u` miễn phí (trước đây cố tình bỏ qua). **Chủ đích không đụng** vào dispatch key của `QueryScreenComponent` cho Sidebar/Results (nó vẫn tự match `g`/`G`/`Ctrl-d`/`Ctrl-u` theo `focus`) — phần đó đan xen với việc forward key cho editor theo cách được test kỹ, rủi ro đổi không đáng so với lợi ích dedupe thêm.
   - **Bỏ `edtui`, tự viết editor** — xong. Lý do: `edtui` chỉ hỗ trợ syntax highlighting qua `syntect` (không có điểm cắm treesitter, `highlight_line` là `pub(super)` gắn cứng syntect); user chọn bỏ hẳn `edtui` thay vì fork hay chỉ tự vẽ layer màu lên trên (`AskUserQuestion`, 2 vòng làm rõ). `QueryEditorComponent` viết lại từ đầu: buffer `Vec<Vec<char>>`, `EditorMode::{Normal, Insert}` (không có Visual/registers/macros/counts), di chuyển hàng (`j`/`k`/`gg`/`G`/`Ctrl-d`/`Ctrl-u`) tái dùng `tradar_core::vim_list`, cộng `h`/`l`/`0`/`$`/`i`/`a`/`I`/`A`/`o`/`O`/`x`/`dd`. API public giữ nguyên (`text`/`set_text`/`insert_at_cursor`/`forward_key`/`draw`) nên `QueryScreenComponent` chỉ cần đổi `.state.mode` → `.mode` ở 5 chỗ — toàn bộ ~30 test tích hợp cũ của nó pass không sửa gì thêm. Cursor giữ nguyên column khi di chuyển hàng rồi clamp vào độ dài dòng mới (đúng hành vi vim). Verify tay qua tmux: insert/Esc/hjkl/gg/G/o/dd/F5 chạy query đều đúng. **Ghi chú kiến trúc phát sinh**: constraint "`Component` không thể `Send`" trong `docs/architecture.md` (mục mô tả `ConnectOutcome`) từng do `edtui::EditorState` giữ `Rc<RefCell<dyn ClipboardTrait>>` — giờ `edtui` đã bị bỏ, đáng kiểm tra lại xem `Component: Send` có khả thi không (chưa làm, chưa có nhu cầu cụ thể, xem "Đã cân nhắc và gác lại" trong `docs/architecture.md`).
   - **Treesitter syntax highlighting cho SQL (phase 2)** — xong (2026-08-12). Crate mới `crates/tradar-query-workbench/src/sql_highlight.rs`, dùng `tree-sitter-highlight` + `tree-sitter-sequel` (grammar SQL của DerekStride/tree-sitter-sql, dùng `HIGHLIGHTS_QUERY` bundle sẵn trong crate thay vì tự viết query). API: `char_colors(text: &str) -> Option<Vec<Color>>` — một màu cho mỗi **char** (không phải byte, map lại qua `text.char_indices()` để khớp buffer `Vec<Vec<char>>` của editor). `QueryEditorComponent` có thêm `Dialect::{PlainText, Sql}` (mặc định `PlainText`), `QueryScreenComponent::new()` set `Sql` nếu `driver` là `"postgres"`/`"sqlite"` — Mongo/Elasticsearch/Redis dùng cú pháp tự chế không có grammar khớp, giữ nguyên plain text. Màu tính lại mỗi lần `draw()` (rẻ với query cỡ bình thường, với lại `draw()` giờ đã được chặn sau cờ `dirty`, không chạy tràn lan). **Bug phát hiện lúc verify tay qua tmux**: `(comment) @comment @spell` — grammar gắn 2 capture lên cùng 1 node, capture nào thực sự fire không đảm bảo, ban đầu làm comment bị tô nhầm màu string (do lỡ gộp `"spell"` chung nhóm màu với `"string"`) rồi sau đó mất màu hẳn (khi thử fix bằng cách "kế thừa màu bên dưới" — hoá ra chỉ `@spell` fire, không có `@comment`, nên kế thừa từ stack rỗng ra `Reset`); fix đúng là map cả `"comment"` và `"spell"` cùng một màu (`DarkGray`), không phụ thuộc capture nào thắng. **Giới hạn đã biết, ghi trong code**: predicate `#match?` của `@number`/`@float` trong `highlights.scm` dùng cú pháp Lua pattern (`%d+`, viết cho highlighter Lua của Neovim) — không khớp regex engine của `tree-sitter-highlight` (Rust), nên số luôn rơi về màu `@string` thay vì màu riêng; cosmetic, không đáng fork query file để sửa.
   - **Theme + keymap tuỳ biến + polish UI** — xong (2026-08-13). User yêu cầu "UI thật ngon, dễ dùng, dễ customize keymap"; scope chốt qua `AskUserQuestion`: keymap remap được ở mức app-action + list nav (**không** đụng phím vim trong editor — đó là vim chuẩn, remap sai thì tự khoá mình), và một theme dark mặc định + override từng màu (không làm nhiều preset).
     - **`tradar_core::theme`** — bảng màu theo *vai trò* (`border_focused`, `error`, `syntax_keyword`...), không component nào tự đặt màu nữa. Mặc định dùng màu 256-color indexed (không phải truecolor) để không vỡ trên terminal cũ. Override từng key qua `[theme]` trong config; key sai hoặc màu không parse được là **lỗi**, không phải no-op im lặng (typo mà im lặng thì user chỉ thấy "config của tôi không ăn").
     - **`tradar_core::keymap`** — `Command` (24 lệnh) × `Context` (`global`/`picker`/`query-screen`/`list`/`prompt`). Component hỏi "phím vừa bấm là lệnh gì ở đây" thay vì tự match `KeyCode`. Hỗ trợ chuỗi 2 phím (`gg`) qua `resolve_in(contexts, &mut pending, key)` — `pending` do component giữ, chính là `pending_g` cũ được tổng quát hoá. Một lệnh gắn được nhiều phím (`j` và `down`), và bind rỗng = gỡ phím.
     - **`tradar_core::config`** — `~/.config/tradar/config.toml`, `[theme]` + `[keymap.<context>]`, value là một chuỗi hoặc mảng chuỗi. Config hỏng thì **cảnh báo ra stderr rồi chạy tiếp bằng default**, không chết app (mất màu không đáng để chặn người ta vào database).
     - **`tradar_core::ui`** — widget dùng chung: `panel()` (viền bo tròn + màu theo focus), `selection_style()`, `centered_rect()`, hint bar dưới đáy, và `HelpOverlay`. Đặt ở `tradar-core` vì cả `tradar-app` lẫn `tradar-query-workbench` đều vẽ, mà hai bên không được phụ thuộc nhau.
     - **Polish**: viền bo tròn + màu viền/tiêu đề đổi theo focus (bỏ hậu tố `[focused]` trong title), status bar 1 dòng ở đáy sinh **từ keymap đang chạy** (remap xong hint tự đổi theo, không hardcode), overlay `?` liệt kê toàn bộ binding hiện hành (cũng sinh từ keymap — không có cheatsheet chép tay để lệch), tab bar có màu, results hiện số dòng ("1 row"/"3 rows"), editor có mode badge `NORMAL`/`INSERT` + vị trí con trỏ `dòng:cột`, sidebar hiện số lượng entry, layout editor/results co giãn theo chiều cao terminal.
     - **Hai quy tắc giữ cho remap không thành tự bắn chân**, cả hai có test: (1) ký tự thường gõ trong Insert mode **luôn là text**, không bao giờ là lệnh (nếu không thì bind `?` sẽ làm không gõ được dấu `?`); (2) lệnh thuộc về một pane cụ thể (`yank` → Results, `insert-name` → Sidebar) khi bấm ở chỗ khác thì rơi xuống editor như cũ — nên `enter` vẫn xuống dòng bình thường.
     - Verify tay qua tmux với một `config.toml` thật (remap `move-down` → `n`, `new-tab` → `ctrl-n`, đổi 2 màu): status bar và viền đổi theo, `j` hết tác dụng; rồi thử config hỏng để xác nhận nó cảnh báo và chạy tiếp bằng default. 201 test pass.
   - **Bảng kết quả căn cột** — xong (2026-08-13). Phát hiện khi test tay: `ResultsComponent` nối cột bằng 2 dấu cách nên cột không bao giờ thẳng hàng (`id  name  n` / `1  alice  10` / `1000  bo  2000`) — với công cụ database thì đây là chỗ dễ thấy nhất, vì bảng kết quả là thứ nhìn nhiều nhất. Giờ dùng widget `Table` của ratatui với độ rộng tính theo giá trị rộng nhất của từng cột, **giới hạn 40 ký tự** (`MAX_COLUMN_WIDTH`) để một cột text dài không đẩy hết cột khác ra ngoài màn hình; ô bị cắt có `…` để không nhầm với giá trị đầy đủ. Bảng nhiều cột cuộn ngang theo cột bằng 2 lệnh keymap mới `scroll-left`/`scroll-right` (`h`/`l` + mũi tên), gate qua `required_focus` = Results nên ở chỗ khác chúng vẫn là phím của editor.
5.5. **Màn hình add/edit/delete connection** — xong (2026-08-14). Trước đó muốn thêm connection phải sửa tay `connections.toml` rồi khởi động lại, và file rỗng thì app in thông báo rồi thoát luôn (dead end). Giờ picker có `a` thêm, `e` sửa, `d` xoá (hỏi xác nhận bằng `y` vì nó ghi vào file), và mở app khi chưa có connection nào sẽ vào thẳng picker rỗng để thêm.
   - Form 3 field: name, target, và driver — driver là **picker** chạy trên đúng danh sách connector id được compile vào bản build này (`main.rs` truyền xuống từ registry qua `RootComponent::with_editing`), không phải text tự do: gõ sai id thì mãi tới lúc connect mới báo "unknown connector". Name/target rỗng bị từ chối kèm việc focus lại đúng field sai.
   - Lưu ghi thẳng cả file ngay khi confirm — không có trạng thái "chưa lưu" để phải giải thích và làm sai. Lỗi ghi file hiện trong hộp Error của picker chứ không nuốt im lặng.
   - Ô nhập text một dòng được tách từ `FilePromptComponent` ra `tradar_core::ui::TextInput` để cả hai dùng chung (con trỏ, phím sửa text, vẽ block cursor) — đúng nguyên tắc "reuse shared UI components" trong `CLAUDE.md`.
   - **Hệ quả cần biết**: lưu từ UI ghi lại `connections.toml` dạng TOML chuẩn, nên comment và thứ tự key tự sắp trong file viết tay sẽ mất.
   - Verify tay qua tmux trên config thật: thêm một connection sqlite qua form → connect vào được → xoá đi → file trở lại đúng 5 entry ban đầu.
   - Còn lại chưa làm (chưa được scope, đừng tự bịa): nhiều theme preset dựng sẵn, remap phím vim trong editor, tìm kiếm trong results, và cột resize được bằng tay.
6. **Migration sang kiến trúc connector pluggable** — xong (2026-08-10), cả 4 bước. Shape mục tiêu (`Connector → Session → Screen`, tách Cargo workspace để các hệ thống không có hình dạng query như Kafka/RabbitMQ/Kubernetes có thể thêm vào mà không đụng core) được đặc tả đầy đủ ở mục "Kiến trúc mục tiêu: connector pluggable" trong `docs/architecture.md` — vẫn còn là "mục tiêu" đúng nghĩa cho nhóm connector không có hình dạng query (Kafka/Kubernetes/SSH), vì chưa có connector nào trong nhóm đó được thêm; với 5 connector dạng query hiện có, mục đó giờ mô tả đúng những gì đã dựng. Xuyên suốt: đụng tới cả năm driver hiện có, `RootComponent`, `main.rs`, và `storage`; làm từng bước như dự định, không phải một thay đổi lớn duy nhất.
   - **Bước 1 (tách workspace thuần cơ học)** — xong (2026-08-08). Dựng Cargo workspace 2 crate: `tradar-core` (nhận `storage/` + `config/`, hai module duy nhất không phụ thuộc gì khác) và `tradar-app` (binary `tradar`, nhận `action.rs`/`components/`/`drivers/`/`query_engine/`/`main.rs`, phụ thuộc `tradar-core` cho `SavedConnection`/`DriverKind`/`ConnectionStore`). `Action`/`Component` **không** chuyển vào `tradar-core` như kế hoạch ban đầu dự định — `Action` còn phụ thuộc trực tiếp `QueryEngine`/driver type nên tách sẽ tạo cycle; việc này dời sang bước 3. Đã verify: `cargo build`, `cargo fmt --check`, `cargo clippy --all-targets --workspace -- -D warnings`, full test suite (94 test), `cargo run` (binary vẫn tên `tradar`). Chi tiết đầy đủ ở `docs/next-run.md`.
   - **Quyết định 2026-08-08 (trước khi bước 3 bắt đầu):** `Connector`/`Session`/`ConnectorDescriptor` sẽ vào một crate riêng `tradar-connector-api`, không phải `tradar-core`, để tránh `tradar-core` phình thành "god crate". Xem `docs/architecture.md` mục "Bố cục workspace".
   - **Quyết định 2026-08-09 (gộp bước 2 + đầu bước 3):** khi bắt tay vào bước 2 (tách `tradar-query-workbench`), phát hiện nó bị phụ thuộc vòng vào phần đầu bước 3 (thu hẹp `Action`) — `tradar-query-workbench` không được phép phụ thuộc `tradar-app` (nơi `Action`/`Component` đầy đủ đang sống), nhưng `QueryScreenComponent`/`QueryEngine` lại implement/dùng đúng hai kiểu đó, nên không thể tách crate trước khi `Action` đã được thu hẹp và chuyển xuống `tradar-core`. Người dùng chọn gộp hai việc làm một lượt (thay vì chỉ làm phần thu hẹp `Action` trước rồi tách crate sau); bước 4 (migrate từng driver thành connector crate riêng) vẫn để sau.
   - **Bước 2 + đầu bước 3 (gộp)** — xong (2026-08-09). Đã dựng: `tradar-core` nhận `action.rs` (enum `Action` đóng 5 variant: `Quit`/`OpenRequested`/`Opened`/`OpenFailed`/`BackToPicker`, đổi tên `Connect*`→`Open*`; trait `Component` thêm `tick()` mặc định no-op) và `capability.rs` (enum `Capability`, chưa dùng ở đâu — đúng dự tính); crate mới `tradar-connector-api` (trait `Connector`, trait `Session`, `ConnectorDescriptor`); crate mới `tradar-query-workbench` nhận `QueryScreenComponent`/`ResultsComponent`/`SchemaSidebarComponent`/`QueryEditorComponent`, `QueryEngine` (implement `Session`), trait `QueryDriver` (đổi tên từ `Driver`) + `SchemaInfo`/`QueryResult`. `tradar-app`: `RootComponent.query_screen` cố định → `ScreenSlot::ConnectionPicker | Active(Box<dyn Component>)`; 5 driver hiện có đổi sang implement `QueryDriver`; `connectors.rs` mới với 5 adapter mỏng (`PostgresConnector`, ...) implement `Connector`, vẫn match theo `DriverKind` cũ (registry `String`-id thật sự là việc của bước 4, xem `docs/architecture.md` mục "Registry"). Verify: `cargo build --workspace`, `cargo clippy --all-targets --workspace -- -D warnings`, `cargo fmt --check`, `cargo test --workspace --lib` (152 test qua khi bỏ qua các test cần Docker; test Postgres/Elasticsearch cần Docker chạy qua khi test tuần tự — 3 test Elasticsearch từng fail khi chạy song song do testcontainers tranh tài nguyên, pass lại khi chạy `--test-threads=1`, không liên quan tới thay đổi lần này), `cargo run` (khởi động được, vào tới connection picker qua pty giả lập bằng `script`). Hai điểm lệch so với đặc tả gốc, phát hiện khi làm thật — chi tiết đầy đủ ở `docs/architecture.md`:
     1. `Action::Opened` không thể được dựng ngay trong task `tokio::spawn` của connect, vì `Component` không (và không thể) là `Send` — `QueryEditorComponent` giữ `edtui::EditorState`, bên trong có `Rc<RefCell<dyn ClipboardTrait>>`. Giải quyết bằng một channel nội bộ riêng trong `main.rs` (`ConnectOutcome`, không đi qua `tradar-core`) mang `Box<dyn Session>` (đã `Send + Sync`) về; `Screen` được dựng bằng `session.build_screen(...)` ngay trên event loop, không phải trong task spawn.
     2. `Action::ExportCurl` bị loại khỏi `Action` đúng dự tính; tại thời điểm đó chưa đạt hình dạng cuối "xử lý hoàn toàn trong Session/Screen riêng của Elasticsearch" vì Elasticsearch chưa tách connector crate riêng — bản trung gian là `QueryDriver::export_curl()` (mặc định `None`, Elasticsearch override). Đã đạt hình dạng cuối sau khi Bước 4 xong (xem bên dưới).
   - **Bước 4 (migrate từng driver thành connector crate riêng + registry)** — xong (2026-08-10). Dựng 5 crate mới dưới `crates/connectors/`: `tradar-postgres`, `tradar-sqlite`, `tradar-elasticsearch`, `tradar-redis`, `tradar-mongo` — mỗi crate chuyển nguyên driver + test tương ứng từ `tradar-app/src/drivers/*`, cộng một struct `Connector` adapter (không `pub`) và một hàm `pub fn connector() -> Box<dyn Connector>` export duy nhất; `ElasticsearchDriver::export_curl` giờ nằm trọn trong `tradar-elasticsearch`, đạt đúng mức cô lập mà bước 2+3 còn nợ. `tradar-core::storage::SavedConnection.driver` đổi từ enum `DriverKind` sang `String` (format `connections.toml` trên đĩa không đổi, vì serde vốn đã serialize `DriverKind` thành cùng chuỗi thường). `tradar-app/src/main.rs` có thêm `registry()` dựng `HashMap<String, Box<dyn Connector>>` từ 5 hàm `connector()`, bọc `Arc` để chia sẻ giữa các task connect; id không khớp → `Action::OpenFailed` runtime (`"unknown connector '{id}': not compiled into this build"`). `tradar-app/src/drivers/` và `connectors.rs` bị xoá hoàn toàn — `tradar-app` giờ không còn chứa code driver nào. Verify: `cargo build --workspace`, `cargo clippy --all-targets --workspace -- -D warnings`, `cargo fmt --check`, `cargo test --workspace --lib` qua trên cả 10 crate kể cả các test cần Docker (Postgres/Elasticsearch/Redis/Mongo) — 3-4 test Elasticsearch flake khi container Docker start chậm/tranh tài nguyên (từng thấy cả `StartupTimeout` lẫn `SocketNotFoundError("/var/run/docker.sock")` thoáng qua), pass lại khi chạy riêng/tuần tự, không phải regression từ thay đổi lần này (driver logic chuyển nguyên khối, không sửa). `cargo run` khởi động được, vào tới connection picker qua pty giả lập bằng `script`. Không phát sinh lệch nào so với "Registry" trong `docs/architecture.md` — theo đúng đặc tả.

## Yêu cầu mới (2026-08-14) — 7/8 xong

User rà lại sau khi dùng thử và chốt danh sách dưới đây. Thứ tự làm đã thống nhất theo phụ thuộc kỹ thuật, không phải theo độ khó.

1. ~~**Phản hồi cho câu lệnh không phải SELECT**~~ — xong (2026-08-14). Thêm `QueryResult::Affected { rows }`, hiện "OK — 3 rows affected". SQL driver chọn nhánh qua `query_driver::returns_rows` — heuristic theo keyword đầu câu (có xử lý `RETURNING` vì `INSERT ... RETURNING` *có* trả về dòng, và bỏ qua comment đầu câu để không bị che mất động từ), đặt ở `tradar-query-workbench` vì cả hai SQL connector đều cần mà connector không được phụ thuộc lẫn nhau. Mongo/Redis/Elasticsearch vốn đã trả về reply có nghĩa nên không đụng.
2. ~~**Schema sidebar xem được cột**~~ — xong (2026-08-14). `SchemaInfo` có thêm `columns: Vec<ColumnInfo { name, type_name }>`; sidebar thành cây mở được: `l`/`→` mở, `h`/`←` đóng (đang ở cột thì đóng bảng cha và đưa selection về đó, không để con trỏ trỏ vào dòng vừa biến mất), `Enter` chèn tên đang trỏ nên giờ chèn được cả tên cột. Postgres lấy bảng + cột trong **một** query join `information_schema`; SQLite không có `information_schema` nên chạy `pragma_table_info` mỗi bảng (chấp nhận được với file local). Mongo/Redis/Elasticsearch trả về rỗng — collection không có schema cố định, key Redis không có cột — và sidebar không vẽ marker mở ở chỗ không có gì để mở. **Kèm theo**: tách `Context::Sidebar` và `Context::Results` khỏi `Context::QueryScreen`; screen chỉ truyền context của panel đang focus vào `resolve_in`, nhờ đó `l` = "mở bảng" ở sidebar và "cuộn phải" ở results mà không đụng nhau, và thay thế hoàn toàn `required_focus` cũ. **Vẫn là tiền đề cho mục 5 (autocomplete)** — dữ liệu cột giờ đã có sẵn để lấy gợi ý.
3. ~~**Giới hạn số dòng + huỷ query đang chạy**~~ — xong (2026-08-14). SQL driver stream (`fetch`) và dừng ở `query_driver::MAX_ROWS` (10.000), đọc dư đúng 1 dòng để biết còn nữa hay không; `QueryResult::Table` có thêm `truncated: bool` để UI nói rõ "first 10000 rows — truncated" thay vì lặng lẽ đưa một phần như thể là toàn bộ. `QueryEngine` giữ `JoinHandle`, `cancel()` abort + tăng epoch (reply đang bay bị bỏ), và results pane hiện "running… ctrl-c to cancel" khi query đang chạy — trước đây query chậm trông y hệt phím không ăn.
4. ~~**Hỗ trợ chuột**~~ — xong (2026-08-14). `main.rs` bật mouse capture và định tuyến `Event::Mouse` qua `Component::handle_mouse_event` mới (mặc định bỏ qua — component chưa biết mình được vẽ ở đâu thì không được đoán). Click vào panel = focus panel đó + chọn dòng dưới con trỏ; bánh xe cuộn **panel dưới con trỏ** chứ không phải panel đang focus (đúng kỳ vọng của chuột); click tab bar để chuyển tab. Overlay (help, file prompt, history, form connection, xác nhận xoá) nuốt click vì click xuyên qua sẽ tác động lên thứ user không nhìn thấy. Hit-test cần scroll offset nên 4 list panel giờ giữ `ListState`/`TableState` giữa các frame — tiện thể giữ luôn vị trí cuộn qua mỗi lần vẽ lại. Verify end-to-end bằng cách chạy binary thật qua pty (lưu ý: pty từ `pty.fork()` khởi đầu 0×0, phải `TIOCSWINSZ` trước, không thì app không vẽ gì để mà click).
5. ~~**Autocomplete + gợi ý cú pháp, theo từng database**~~ — xong (2026-08-14). Gõ trong Insert mode thì popup gợi ý hiện ngay dưới con trỏ; `tab` chấp nhận, `ctrl-n`/`ctrl-p` (hoặc mũi tên) chuyển gợi ý, và `tab` **giữ nguyên** nghĩa cũ (chuyển focus) khi không có popup. Hai nguồn gợi ý, cả hai đều không để `tradar-query-workbench` biết driver nào tồn tại: (a) `QueryDriver::keywords()` — method mới, **mỗi connector tự khai vocab của mình** (Redis khai lệnh Redis, Mongo khai `db`/`find`/`$match`..., ES khai verb HTTP + Query DSL; hai connector SQL dùng chung `query_driver::SQL_KEYWORDS` — được phép vì cả hai đều phụ thuộc crate này, còn connector thì không được phụ thuộc lẫn nhau); (b) tên bảng/cột từ `SchemaInfo` — đây chính là thứ mục 2 mở khoá. Tên trong schema **xếp trên** keyword: keyword là danh sách cố định đoán được, tên bảng của bạn thì không. Khớp không phân biệt hoa thường, không gợi ý khi từ đã gõ đủ (khớp chính xác). Thêm `Context::Completion` vào keymap nên remap được. **Kèm theo, sửa một bug lọt từ commit chuột**: dòng gán `editor_area` không được áp dụng (patch khớp nhầm dạng code trước khi `rustfmt` chạy) nên `editor_area` luôn bằng 0 — click vào editor không focus được, và popup neo vào góc trái màn hình thay vì con trỏ; cả hai giờ đã có test hồi quy.
6. ~~**Panel navigator nhiều connection cùng lúc**~~ — xong (2026-08-14). Phạm vi chốt qua `AskUserQuestion` trước khi code, hai câu:
   - **Navigator chỉ dẫn đường, `Tab` vẫn gắn với đúng một `Session`.** Không dựng pool Session trong một tab — đó là hướng còn lại đã cân nhắc và bỏ, vì nó bắt phải trả lời "query đang gõ dở và kết quả cũ thuộc về connection nào" mà chẳng đổi lại được gì user thực sự cần. Chọn một connection trong navigator = connect nó vào một tab (tái dùng tab hiện tại nếu tab đó còn đang ở picker) hoặc nhảy tới tab đã mở sẵn; chọn một bảng/cột = nhảy tới tab của connection đó rồi chèn tên vào editor. Rủi ro thấp hơn hẳn và giữ nguyên toàn bộ cơ chế epoch/tab đã có.
   - **Cây `connection → bảng → cột`**, bỏ cấp `database` giữa: mỗi `SavedConnection` vốn đã trỏ vào một db/schema cụ thể trong `target`, nên thêm cấp đó sẽ phải thêm method "liệt kê database" vào `QueryDriver` và implement cho cả 5 connector (Elasticsearch còn không có khái niệm này). Connection chưa connect hiện mờ và không bung được — bấm mở chính là connect, nên không có trạng thái "thấy schema mà chưa kết nối".
   - **`SchemaSidebarComponent` bị xoá hẳn**; cây schema chuyển lên app shell thành `crates/tradar-app/src/components/navigator.rs`. Bắt buộc phải ở `tradar-app`: chỉ `RootComponent` mới biết có những connection nào khác, cái nào đang mở và ở tab nào. Giữ cả hai (sidebar trong screen + navigator) sẽ là cùng một bảng hiện hai lần trên một màn hình, mà chỉ một trong hai với tới được connection khác.
   - **Cách navigator lấy được schema mà không phá cách ly connector**: `Component` (ở `tradar-core`) thêm 3 method mặc định — `outline() -> Vec<OutlineEntry>` (screen tự làm phẳng cấu trúc của mình thành `{depth, label, detail, has_children}`; app shell vẽ mà không cần biết "bảng"/"cột" là gì), `outline_error()`, và `insert_text(&str)`. Cùng contract với `restore_state` đã có: app chỉ chuyển tiếp, không hiểu nội dung. `QueryScreenComponent` implement chúng từ `engine.schema()`.
   - **Phím**: `ctrl-b` (`Command::ToggleNavigator`, context `global`) xoay vòng 3 trạng thái ẩn → hiện+focus → ẩn, nên cùng một phím luôn đưa về chỗ cũ. Trong navigator: `j`/`k`/`gg`/`G`/`ctrl-d`/`ctrl-u`, `l`/`→` mở, `h`/`←` đóng (đóng node con thì lùi con trỏ về node cha, không để trỏ vào dòng vừa biến mất), `enter` chọn, `esc` trả focus. **Phím nào navigator không nhận thì tự trả focus lại cho screen** thay vì nuốt — một panel ăn mọi phím là panel không thoát ra được. Click/cuộn chuột trong panel cũng hoạt động.
   - **Hệ quả trên query screen**: `Focus` chỉ còn `Editor`/`Results`, `Tab` xoay vòng 2 pane. *Lưu ý tương thích*: context keymap `sidebar` đổi tên thành `navigator` — ai đã remap trong `config.toml` sẽ thấy lỗi "unknown context", các binding thì giữ nguyên.
   - Verify tay qua tmux với 2 connection sqlite: `ctrl-b` mở cây, bung connection đang mở ra bảng → cột (thấy cả marker `pk`), `l` trên connection thứ hai (chưa connect) thì nó tự connect và mở tab mới, rồi `enter` trên bảng của connection đó chèn tên vào đúng editor của tab đó và query chạy đúng trên connection thứ hai.
7. ~~**Bảng kết quả thao tác được như bảng dữ liệu thật**~~ — xong (2026-08-14), làm trọn cả ba gạch đầu dòng trong một lượt theo yêu cầu user.
   - **Cột số thứ tự dòng** — `ResultsComponent` vẽ thêm một cột gutter đầu bảng (header `#`, màu `text_dim`), rộng đúng theo số chữ số của dòng cuối. Không nằm trong `column_spans` nên không bao giờ trở thành "cell đang chọn".
   - **Di chuyển giữa các cell** — thêm `selected_col` bên cạnh `selected`, render qua `TableState::select_cell` + `cell_highlight_style` (mới: `ui::cell_cursor_style()`, là `selection_style()` cộng `REVERSED` — không thêm role màu mới, và luôn nhìn thấy được dù user đổi `selection_bg` thành gì). **Thay hẳn** hai lệnh keymap `scroll-left`/`scroll-right` bằng `prev-column`/`next-column` (vẫn `h`/`l` + mũi tên): cuộn ngang giờ là **hệ quả** — `draw()` chỉ cuộn `col_offset` vừa đủ để cell cursor còn trên màn hình, và chỉ render số cột thực sự vừa (`fitting_columns`), vì render cột không vừa sẽ khiến layout co mọi cột lại và bảng hết thẳng hàng. Click chuột giờ chọn đúng **cell** chứ không chỉ dòng. *Lưu ý tương thích*: ai đã remap `scroll-left`/`scroll-right` trong `config.toml` sẽ thấy lỗi "unknown command" — đổi sang tên mới.
   - **Sửa cell / xoá dòng** — đi theo hướng user chọn: **sinh SQL rồi execute**, không ghi thẳng. `enter` (trong Results) sửa cell, `d` xoá dòng; cả hai mở overlay mới `components/row_edit.rs` — sửa thì gõ giá trị mới trước, rồi **luôn hiện đúng câu lệnh sắp chạy** và đòi `y` (cùng idiom với xác nhận xoá connection ở picker), phím khác là huỷ. Câu lệnh chạy qua `engine.submit_all(vec![edit, select_cũ])` nên **bảng tự đọc lại ngay sau khi sửa** thay vì biến thành "OK — 1 row affected"; `set_result_keeping_cursor` giữ nguyên cell cursor để không bị nhảy về đầu bảng. Lệnh sửa cũng vào query history như mọi câu khác.
   - **Cách biết bảng nguồn + khoá chính** (phần "cần thiết kế thêm" của yêu cầu gốc): hai method mới trên `QueryDriver` — `edit_source(query)` (bảng mà result đọc từ đó) và `edit_sql(&RowEdit)` (câu lệnh) — mặc định `None` = bảng kết quả chỉ đọc, nên Mongo/Redis/Elasticsearch không bị đụng. Hai connector SQL uỷ quyền cho helper dùng chung trong `query_driver.rs`: `single_table_source` (lexer đếm độ sâu ngoặc, **cố tình bảo thủ** — join, union, group by, distinct, subquery, hay bất cứ thứ gì nó không đọc chắc chắn đều trả `None`, vì đoán sai nghĩa là sinh `UPDATE` nhắm sai bảng) và `build_sql_edit` (quote identifier theo từng phần dotted, escape `'` thành `''`, `NULL` viết không quote đúng như grid hiển thị). Khoá chính lấy từ `SchemaInfo`: `ColumnInfo` thêm field `primary_key` — Postgres lấy trong cùng một query join `information_schema.key_column_usage`, SQLite đọc cột `pk` của `pragma_table_info` (khác 0 = thuộc khoá, hỗ trợ luôn khoá phức hợp). Sidebar hiện marker `pk` để thấy trước khi thử.
   - **Mọi trường hợp từ chối đều nói rõ lý do** trong chính overlay đó (không phải bảng đơn / bảng không có khoá chính / cột khoá không có trong result / schema đọc lỗi) — một phím bị từ chối im lặng thì không phân biệt được với phím hỏng.
   - Verify tay qua tmux trên sqlite thật: sửa `name` của dòng 2 → thấy đúng `UPDATE "users" SET "name" = 'Linda' WHERE "id" = '2'` → `y` → DB đổi thật và bảng tự refresh; xoá dòng tương tự; query có `JOIN` thì bị từ chối kèm giải thích. **Chưa verify được trên Postgres** (máy không có Docker) — phần thay đổi bên đó là query `list_schema` mới lấy thêm cờ khoá chính, chưa có test nào chạy qua.

8. **Xem lại toàn bộ keymap** — user nói "phase đầu xây dựng chưa ưng phần mapping bằng bàn phím", sẽ dùng thử rồi phản hồi cụ thể. **Đừng tự đổi binding khi chưa có phản hồi đó** — hạ tầng remap đã có sẵn (`tradar_core::keymap` + `config.toml`), nên đổi mặc định là việc rẻ khi đã biết muốn đổi gì.

## Query file: giữ nội dung + nhiều câu lệnh (user nêu 2026-08-14) — xong

Ưu tiên ngay sau schema Elasticsearch. Hai vấn đề tách biệt nhưng cùng đụng vào query editor:

1. ~~**Không giữ lại nội dung query.**~~ Xong (2026-08-14). Chọn hướng **lưu buffer vào session file** (không gắn tab với file `.sql` — đơn giản hơn và không phát sinh câu hỏi "file này của ai"). `SessionState.tabs` đổi từ `Vec<String>` sang `Vec<TabState { connection, query }>`. Để lấy/đưa text qua ranh giới connector mà không giả định screen nào cũng là text: thêm `Component::restore_state() -> Option<String>` (screen tự quyết muốn giữ lại gì, mặc định `None`) và `Session::build_screen(..., restore: Option<&str>)` nhận lại đúng chuỗi đó — app chỉ lưu và trả lại, không hiểu nội dung. **Lưu ý tương thích**: `session.toml` định dạng cũ (mảng chuỗi) sẽ parse lỗi → app rơi về phiên mới; đổi lại chỉ mất đúng một lần restore của file vốn được ghi lại mỗi lần thoát, nên không viết code migrate.
2. ~~**Một file chỉ chứa được một câu lệnh.**~~ Xong (2026-08-14). `F5`/`Ctrl+Enter` chạy **câu tại con trỏ**, `Ctrl+A` chạy **tất cả** (tuần tự, dừng ở câu lỗi đầu tiên và nói rõ câu thứ mấy — chạy tiếp sẽ áp dụng nửa script mà không biết nửa nào). Ranh giới câu do driver tự quyết qua `QueryDriver::split_statements` mới (mặc định "cả buffer là một câu"): SQL dùng chung `query_driver::split_sql_statements` — một lexer tách theo `;` nhưng bỏ qua `;` nằm trong chuỗi `'...'`/`"..."` (kể cả `''` escape), trong comment `--`/`/* */`, và trong dollar-quote `$$`/`$tag$` của Postgres; Redis/Mongo mỗi dòng một câu; Elasticsearch gom `METHOD /path` + body JSON bên dưới thành một request như console của Kibana (nên body pretty-print có dòng trống vẫn nguyên vẹn). Yêu cầu gốc bên dưới giữ nguyên để đối chiếu: Thực tế người ta để cả chục câu trong một file rồi chạy từng câu. Yêu cầu:
   - **Chạy tất cả** (execute all) — chạy tuần tự; cần quyết cách báo kết quả (kết quả câu cuối? tổng hợp? dừng ở câu lỗi đầu tiên?).
   - **Chạy câu tại con trỏ** (inline) — và **tự nhận biết ranh giới câu**, vì một câu có thể trải nhiều dòng nên không thể cắt theo dòng.

   Ranh giới câu là phần khó và **phụ thuộc từng driver**: SQL tách theo `;` nhưng phải bỏ qua `;` nằm trong chuỗi `'...'`/`"..."`, trong comment `--`/`/* */`, và trong dollar-quote `$$...$$` của Postgres; Redis mỗi dòng là một lệnh; Elasticsearch một request là `METHOD /path` + body JSON đi kèm; Mongo mỗi `db.<coll>.<method>(...)` là một câu. Nên đi theo đúng cách đã làm với autocomplete: **thêm một method vào `QueryDriver` để mỗi connector tự tách câu của mình**, mặc định "cả buffer là một câu"; phần SQL dùng chung một helper trong `tradar-query-workbench` như `returns_rows`/`SQL_KEYWORDS`.

## Quản lý file query (user hỏi 2026-08-14) — xong

Trạng thái trước khi làm: `Ctrl+S`/`Ctrl+O` có rồi nhưng **gõ tay đường dẫn**, `last_path` chỉ nằm trong RAM của một screen (mất khi thoát, không chia sẻ giữa tab). Không có thư mục mặc định, không liệt kê được file đã lưu, không có recent list.

Đã làm đúng như hướng chốt:

- **Thư mục mặc định** `~/.config/tradar/queries/` (dùng chung chỗ với `connections.toml`/`session.toml`/`config.toml`) — `storage::default_queries_dir()`. `storage::resolve_query_path()` quyết định: tên trần (`report`) → `queries/report.sql` (tự thêm `.sql` khi chưa có extension); chuỗi có `/`, `\` hoặc bắt đầu bằng `~` → dùng nguyên văn, để người quen đường dẫn tuyệt đối không bị chặn.
- **Recent list** lưu ra `~/.config/tradar/recent.toml` (`RecentFiles`/`RecentStore`), most-recent-first, dedupe khi mở lại (đẩy lên đầu chứ không nhân đôi), cắt ở `MAX_RECENT = 20`; cập nhật cả khi save lẫn khi open. Recent là **toàn cục**, không thuộc session, nên không nhét vào `session.toml`.
- **`Ctrl+O` mở picker** (`components/file_picker.rs`): recent trước (đánh dấu `●`), rồi các file còn lại trong thư mục queries theo thứ tự tên. Gõ để lọc (không phân biệt hoa thường); không khớp gì thì `Enter` mở luôn chuỗi vừa gõ như một đường dẫn — một widget lo cả hai kiểu dùng. File trong recent mà **không còn tồn tại thì bị loại khỏi danh sách** thay vì hiện ra rồi báo lỗi khi mở. File ngoài thư mục queries hiện đủ đường dẫn (chỉ tên file thì không biết là file nào). Mở lỗi thì **giữ overlay** và hiện lỗi, đóng đi thì không còn dấu vết gì báo là mở hụt.
- **Chỗ đặt state**: `queries_dir` + recent list là **global của process** (`storage::init_query_files` / `storage::query_files`), cùng kiểu với `theme()`/`keymap()`. Lý do: screen được dựng sâu bên trong connector (`Session::build_screen`), luồn xuống tận nơi đồng nghĩa với việc nhét "file để ở đâu" vào SPI connector — thứ chẳng liên quan gì tới việc kết nối database. `main.rs` init một lần lúc khởi động; test không init nên `query_files()` trả `None` và code rơi về nhánh gõ đường dẫn tay như cũ.
- Prompt `Ctrl+S` prefill bằng **tên trần** khi file cuối nằm trong thư mục queries (trước đó prefill cả đường dẫn dài, muốn lưu tên khác thì phải xoá hết).

Còn để lại: chưa có xoá/đổi tên file ngay trong picker, chưa đánh dấu buffer "đã sửa chưa lưu", chưa có `Ctrl+S` ghi đè thẳng không hỏi khi đã biết `last_path`.

## Trình duyệt thư mục thật cho Save/Open (user hỏi 2026-08-15) — xong

Trạng thái trước khi làm: `Ctrl+O` liệt kê **phẳng** — chỉ file trực tiếp trong `queries_dir`, thư mục con bị `read_dir` lọc bỏ hoàn toàn, không có cách nào đi vào. `FilePromptComponent` không hiện sẽ lưu/mở vào đâu, chỉ có ô nhập text trần. Chốt qua `AskUserQuestion` 3 hướng (không chọn: tạo thư mục mới ngay trong Save/Open — loại khỏi scope):

- **`FilePickerComponent` thành trình duyệt thật** (`components/file_picker.rs`): field `current_dir` (không còn danh sách cố định dựng một lần trong `new()`), `Entry` phân loại `Recent`/`Dir`/`File`. `Enter` trên thư mục → đi vào (`current_dir` đổi, breadcrumb cập nhật, filter xoá), **không đóng picker**; `Enter` trên file → chọn như cũ. `Backspace` khi ô filter rỗng đi lên một cấp (no-op ở root); còn filter thì `Backspace` vẫn xoá ký tự như trước — chỉ đổi nghĩa khi không còn gì để xoá. Danh sách "gần đây" (`●`) chỉ hiện ở root (`current_dir == queries_dir`) vì đó là đường dẫn tuyệt đối, không thuộc riêng thư mục con nào. Duyệt **giới hạn trong `queries_dir`** — không có `..` thoát ra ngoài; đường dẫn tuyệt đối/tương đối gõ tay vẫn là lối thoát sẵn có riêng (xem điểm `resolve_query_path` bên dưới).
- **`FilePromptComponent` hiện dòng preview** cập nhật theo từng ký tự gõ (`draw()` nhận thêm `queries_dir: Option<&Path>`, gọi thẳng `resolve_query_path` có sẵn) — trả lời trực tiếp "lưu/mở vào đâu" mà không cần confirm mới biết.
- **Nhớ + nhảy nhanh vào thư mục gần nhất**: không thêm storage mới — suy thẳng từ `RecentFiles` đã có (`last_used_dir()`, hàm thuần lấy `&[String]`/`&Path` để test được không cần đụng `QueryFiles` global). `open_file_picker()` giờ mở picker thẳng vào thư mục của file gần nhất thay vì luôn về root; `prompt_prefill()` khi chưa có `last_path` trong phiên (case phổ biến: mới mở app, `Ctrl+S` lần đầu) rơi về thư mục đó, prefill `"<thư_mục>/"` thay vì tên file cũ (tránh hiểu lầm thành ghi đè).
- **Bug phát hiện lúc verify tay qua tmux, đã sửa cùng lúc**: `resolve_query_path()` cũ coi **bất kỳ** chuỗi có `/` là đường dẫn tuyệt đối/nguyên văn theo cwd tiến trình — nghĩa là gõ `reports/first` (đúng định dạng prefill vừa thêm ở trên) lại lưu ra `<cwd>/reports/first` thay vì `queries_dir/reports/first.sql`, hoàn toàn ngoài thư mục quản lý. Sửa: chỉ còn escape thật sự (bắt đầu bằng `/`, `~`, `./`, `../`, hoặc dạng `\`/`.\`/`..\` cho Windows) mới dùng nguyên văn; đường dẫn tương đối không có tiền tố escape (`reports/first`) giờ join vào `queries_dir` và vẫn tự thêm `.sql` như tên trần. Test cũ `anything_that_looks_like_a_path_is_used_as_typed` đổi tên thành `an_explicit_escape_is_used_as_typed`, thêm `a_relative_subfolder_is_joined_into_the_queries_directory`.

Test: `file_picker.rs` (breadcrumb, Enter vào thư mục không đóng picker, Enter vào file vẫn `Chosen`, Backspace lên cấp/no-op ở root, filter chỉ lọc trong thư mục đang đứng, recent chỉ hiện ở root, `start_dir` khác root); `file_prompt.rs` (preview resolve tên trần/subfolder/escape tuyệt đối); `storage/mod.rs` (`last_used_dir`, `resolve_query_path` sau khi sửa); `query_screen.rs` (`Ctrl+O` mở đúng thư mục gần nhất, `Ctrl+S` lần đầu prefill đúng subfolder). Verify tay qua tmux: save vào `reports/first` → xác nhận file nằm đúng `queries_dir/reports/first.sql` (không leak ra cwd); `Ctrl+O` mở thẳng vào `/reports/`; `Backspace` về root thấy cả recent lẫn thư mục `reports/`; mở lại file, nội dung đúng.

Còn để lại: tạo thư mục mới trong Save/Open (đã loại khỏi scope).

## Chưa làm, chưa được scope (2026-08-14)

Gom về một chỗ để không mất: đây là những thứ đã nêu trong lúc trao đổi hoặc phát hiện khi rà code, nhưng **chưa được chốt phạm vi và chưa ai bắt tay vào**.

**Thứ tự ưu tiên user chốt 2026-08-14:** (1) ~~schema Elasticsearch qua `_mapping`~~ *(xong)* → (2) query file: giữ lại nội dung + chứa được nhiều câu lệnh (mục riêng bên dưới) → ... → **export CSV/JSON làm sau cùng**, user nói rõ "chưa quan trọng".

**Tính năng người dùng thấy được**

- ~~**Export kết quả ra CSV/JSON**~~ — xong (2026-08-15), đẩy lên làm sớm hơn dự kiến (2026-08-14 từng hạ ưu tiên xuống cuối) theo yêu cầu user tập trung UI/UX/usability trước connector mới. `Ctrl+E` (context `query-screen`, `Command::Export` mới) mở lại `FilePromptComponent` — tái dùng đúng như dự tính, thêm `PromptKind::Export` thứ ba bên cạnh `Save`/`Open`. Format chọn theo **đuôi file gõ trong prompt** (`.csv`/`.json`), không có overlay chọn format riêng — đuôi khác báo lỗi rõ ràng ngay trong prompt (giữ prompt mở, cùng pattern lỗi với Ctrl+S/Ctrl+O). Khác với `Save`/`Open`, một tên trần **không** rơi vào thư mục queries mặc định — export không phải là query nên không có khái niệm thư mục mặc định cho nó, tên trần chỉ là đường dẫn tương đối theo cwd; thành công cũng **không** ghi vào `last_path`/recent-files list (tránh làm ô nhiễm prefill của Ctrl+S và danh sách Ctrl+O). Luôn xuất **toàn bộ result gốc**, bỏ qua filter `/` đang áp dụng (chốt qua `AskUserQuestion`) — "export cái tôi hỏi" chứ không phải "export cái đang nhìn thấy", tránh xuất thiếu mà không biết nếu quên tắt filter. Logic shape nằm ở module thuần mới `crate::export` (`to_csv`/`to_json`, không phụ thuộc UI) — CSV chỉ nhận `QueryResult::Table` (RFC 4180 tối giản: quote khi có dấu phẩy/quote/newline, quote lồng nhân đôi), `Documents` báo lỗi rõ ràng gợi ý dùng `.json` thay vì tự bịa cách làm phẳng JSON lồng nhau; JSON nhận cả `Table` (mảng object `{cột: giá trị}` mỗi dòng) lẫn `Documents` (pretty-print nguyên văn qua `serde_json`). `Affected` (câu lệnh ghi, không phải `SELECT`) báo "nothing to export" ở cả hai format. Test: unit cho `crate::export` (CSV escape/quote lồng nhau, JSON round-trip, cả hai lỗi rõ ràng cho `Documents`/`Affected`); integration trong `query_screen.rs` (`tempfile`, không cần Docker) cho luồng `Ctrl+E` đầy đủ — xuất CSV, xuất JSON, đuôi lạ giữ prompt mở báo lỗi, không có result báo lỗi, và xác nhận export không đụng `last_path`.
- ~~**Tìm kiếm trong kết quả**~~ — xong (2026-08-15). Chọn **lọc** chứ không phải nhảy giữa các match: với một result không nhìn hết được thì "chỉ hiện những dòng này" mới là câu hỏi thật, và nó ghép được với mọi thứ khác của grid (yank/sửa/xoá đều tác động lên dòng dưới con trỏ, dù đang lọc hay không). `/` (context `results`) mở một thanh nhập một dòng ở đáy panel — thanh chứ không phải overlay giữa màn hình, vì nó lọc đúng thứ nằm phía sau nên che đi là mất luôn feedback duy nhất. Lọc **ngay khi gõ**, không phân biệt hoa thường, khớp bất kỳ cột nào (với `Documents` thì khớp trên JSON text). `Enter` giữ filter và đóng thanh, `Esc` **huỷ hẳn** filter (đưa grid về đúng như trước khi bấm `/`); `/` lần sau prefill filter đang áp dụng nên là tinh chỉnh chứ không phải gõ lại. Tiêu đề panel hiện cả hai con số (`2 of 6 rows — filter: hanoi`) để không bao giờ đọc nhầm view đã lọc thành toàn bộ result. **Hai điểm đúng đắn quan trọng, đều có test**: (1) cột số thứ tự vẫn là vị trí trong result gốc (dòng 1/4/6), không đánh lại từ 1 — "dòng 4" phải luôn nghĩa là dòng thứ 4 của query; (2) `selected` index vào danh sách *đã lọc* và mọi thứ đọc "dòng đang chọn" đều đi qua `visible_items()`, nên `d` trên dòng thứ hai đang thấy sinh `DELETE ... WHERE id = '4'` chứ không phải id của dòng thứ hai trong result gốc. `set_result` xoá filter (query mới mà vẫn lọc thì đang đọc một tập con mà không biết), còn `set_result_keeping_cursor` giữ filter (đó là đọc lại chính query đó sau khi sửa).
- ~~**Trạng thái connection**~~ — xong (2026-08-15). Trước đây không có cách biết một connection còn sống hay đã rớt cho tới khi chạy query và nhận lỗi. `QueryDriver` có thêm `ping()` (mặc định `Ok(())` — coi như còn sống, mỗi connector thật tự implement cú "còn đó không" rẻ nhất của riêng nó: Postgres/SQLite `SELECT 1`, Mongo lệnh `ping`, Redis `PING`, Elasticsearch `GET /`). `QueryEngine::tick()` tự bắn một `ping()` nền mỗi `PING_INTERVAL` (15s, dùng `tokio::time::Instant` để test được với clock giả lập), tối đa một ping bay cùng lúc (`ping_in_flight` chặn chồng ping khi backend chậm/treo), kết quả cập nhật `alive: bool` đọc qua `QueryEngine::alive()`. `Component` có thêm `connection_alive() -> Option<bool>` (mặc định `None` = "không áp dụng", để picker/overlay không phải giả vờ có trạng thái); `QueryScreenComponent` trả `Some(engine.alive())`. Hiện ở hai chỗ: badge `● disconnected` màu đỏ ở dòng mode-bar của editor + viền panel chuyển đỏ (chỉ khi rớt — còn sống thì im lặng, không thêm chữ "connected" thừa), và navigator gắn `✗ disconnected` sau tên connection đang mở tab. **Bug tiện thể bắt được lúc nối dây**: `QueryScreenComponent::tick()` trước đó `std::mem::take(&mut self.refreshing)` vô điều kiện mỗi khi có "thay đổi" — giờ `tick()` cũng trả `true` khi chỉ riêng ping đổi trạng thái (không có outcome query nào), nên nếu giữ nguyên code cũ, một ping xen giữa lúc đang chờ kết quả refresh sau khi sửa cell sẽ xoá cờ `refreshing` sớm và làm bảng nhảy về đầu thay vì giữ nguyên cell cursor; sửa bằng cách chỉ `take` cờ đó bên trong nhánh thực sự có `outcome`. Verify tay qua tmux: connect Postgres thật trong Docker, `docker stop` container, đợi qua 15s → badge + viền đỏ hiện ra ở cả editor lẫn navigator mà không cần chạy query nào.
- **Thêm connector MySQL / MariaDB / ClickHouse.** `README.md` liệt ở mục "Dự kiến". Rẻ nhờ kiến trúc pluggable: thêm crate mới + 1 dòng trong `registry()`, không đụng core. MySQL qua `sqlx` gần như giống hệt connector Postgres đang có.
- ~~**Query snippet CRUD tự sinh từ schema, cả 5 connector**~~ — xong (2026-08-15). User chốt qua `AskUserQuestion`: cả hai hướng snippet (tự sinh **và** thư viện do user tự lưu), nhưng chỉ hướng tự sinh nằm trong lần này — hướng thư viện snippet do user tự đặt tên/lưu/quản lý cần thiết kế storage + UI riêng, đẩy sang mục kế tiếp bên dưới. Đứng trên một bảng/collection/index/key trong navigator (Ctrl+B), `c`/`r`/`u`/`d` (context `navigator`, 4 `Command` mới `CrudCreate`/`CrudRead`/`CrudUpdate`/`CrudDelete`) chèn khung Create/Read/Update/Delete đúng cú pháp connector đó vào editor của tab đang mở connection này — chỉ tác dụng trên hàng **depth 0** (bảng/collection/index/key), không phải cột con hay hàng connection. Kiến trúc tái dùng nguyên 3 pattern đã có, không phát minh gì mới: `QueryDriver::crud_snippet` (optional, mặc định `None`, cùng pattern `edit_sql`/`browse_entry`) → `Component::crud_snippet` (`tradar-core/src/action.rs`, optional, cùng pattern `insert_text`; `CrudOp{Create,Read,Update,Delete}` định nghĩa ở đây chứ không phải `query_driver.rs`, vì `Component` không được phụ thuộc ngược `tradar-query-workbench`) → `NavOutcome::Snippet{tab,name,op}` (cạnh `Insert{tab,name}` đã có, xử lý ở `RootComponent`: chuyển tab, gọi `screen.crud_snippet` rồi **tái dùng nguyên `insert_text`** để chèn — không thêm method "replace buffer" mới). Snippet từng driver: Postgres/SQLite dùng chung `query_driver::build_crud_snippet` (đặt cạnh `build_sql_edit`, cùng `quote_identifier`) — placeholder là `<tên_cột>` chứ không đoán giá trị theo type (không có mapping type→literal đáng tin cậy xuyên mọi backend); MongoDB sinh `db.<collection>.find/insertOne/updateOne/deleteOne`; Elasticsearch sinh `GET/POST/DELETE <index>/...` theo đúng shape Dev-Tools-console driver này đã dùng; Redis tái dùng thẳng `BrowseKind`/`BrowseKind::command` đã có từ mục "Redis: key browser" — Read = y hệt lệnh browse, Create/Update dùng `SET`/`HSET`/`RPUSH`/`SADD`/`ZADD`, Delete = `DEL`, type ngoài 5 loại đã hỗ trợ (stream...) trả `None` giống `browse_entry`. Test: unit cho `build_crud_snippet` (đủ 4 op, có/không pk, không cột nào biết), unit cho `crud_snippet` từng driver (thuần, không cần Docker vì không IO), unit cho `QueryEngine::crud_snippet` (tìm đúng entry, `None` khi không có), unit cho `NavigatorComponent::choose_snippet` (chỉ depth 0), integration test bấm `r` trên navigator trong `tradar-app/components/mod.rs`.
- ~~**Thư viện snippet do user tự lưu/đặt tên**~~ — xong (2026-08-16). Hướng thứ 2 trong yêu cầu gốc của user (2026-08-15), tách khỏi mục tự sinh ở trên. Phạm vi chốt qua `AskUserQuestion` trước khi code (3 câu): lưu **cả buffer** (không phải Visual selection) qua phím mới; thư viện **gắn tag driver lúc lưu**, picker chỉ hiện snippet khớp driver của connection đang mở; UI là **overlay riêng** giống `HistoryPickerComponent`, không gộp vào `Ctrl+O`.
  - **Storage** (`tradar-core/src/storage/mod.rs`): `SavedSnippet { name, driver, text }`, `SnippetStore` (load/save toàn bộ danh sách, cùng pattern `ConnectionStore`) ghi ra `~/.config/tradar/snippets.toml`. `name` chỉ cần *duy nhất trong cùng driver* (đôi khi hai driver khác nhau dùng chung tên, không đụng nhau vì picker không bao giờ hiện cả hai cùng lúc) — lưu lại dưới tên đã tồn tại (khớp cả `name` lẫn `driver`) thì ghi đè, không tạo bản trùng. `Snippets` process-global (`init_snippets`/`snippets()`, `OnceLock` + `RwLock<Vec<SavedSnippet>>`) — cùng lý do `QueryFiles` đã global: `QueryScreenComponent` nằm sâu trong connector, không nên nhét "snippet ở đâu" vào connector SPI. `main.rs` init một lần lúc khởi động, cùng chỗ với `init_query_files`.
  - **Phím**: `Ctrl+K` (`Command::SaveSnippet`) mở prompt một dòng đặt tên (field `ui::TextInput` trần, không phải `FilePromptComponent` — snippet không có khái niệm thư mục/đường dẫn để mà preview), `Enter` lưu `query_editor.text()` với driver của connection hiện tại, `Esc` huỷ. `Ctrl+L` (`Command::OpenSnippets`) mở `SnippetPickerComponent` mới (`crates/tradar-query-workbench/src/components/snippet_picker.rs`) — dựng theo đúng khuôn `HistoryPickerComponent` (constructor nhận `entries` từ caller thay vì tự đọc global, để test được không cần init `Snippets`), thêm hai khả năng nó chưa có: `d` (`Command::DeleteSnippet`) hỏi xác nhận bằng `y` cùng idiom `ConnectionPickerComponent::confirming_delete`, `r` (`Command::RenameSnippet`) mở một `TextInput` prefill tên cũ ngay trong overlay, `Enter` xác nhận đổi tên rồi refresh lại list từ global store. `Enter` trên một snippet nạp `text` vào editor (thay nguyên buffer, giống hệt chọn từ history), không phải chèn tại con trỏ.
  - **`Context::Snippets` mới**, không tái dùng `Context::Prompt` (context đó dùng chung cho mọi ô nhập text trần — `FilePromptComponent`, form connection — nơi chữ cái phải luôn gõ được, không được là lệnh; gán `d`/`r` vào đó sẽ làm hỏng việc gõ tên file/field chứa chữ "d" hay "r"). `ctrl-k`/`ctrl-l` thêm vào `Context::QueryScreen`.
  - Test: unit cho `SnippetStore`/`Snippets` trong `tradar-core` (round-trip, ghi đè theo `(name, driver)`, cùng tên khác driver không đụng nhau, delete/rename chỉ tác động đúng driver, rename không tồn tại là no-op) — dựng `Snippets` trực tiếp bằng store trỏ vào thư mục tạm, không qua `OnceLock` global (cùng lý do `ConnectionStore`/`RecentStore` được test trực tiếp thay vì qua `QueryFiles`). Unit cho `SnippetPickerComponent` (di chuyển, Enter trả `Insert`, Esc trả `Cancelled`, xoá cần `y` xác nhận, đổi tên prefill đúng tên cũ, gõ trong lúc đổi tên không lọt ra ngoài thành lệnh `j`/`d`). Integration trong `query_screen.rs` cho `Ctrl+K`/`Ctrl+L` (mở/đóng prompt và picker, chọn một snippet nạp đúng vào editor) — **`snippets()` không được init trong test** (như `query_files()`), nên các test xác nhận nhánh "không có global store" không panic/không đổi state, giống hệt giới hạn `FilePickerComponent` đã có sẵn với `query_files()`.
  - Cập nhật `README.md` (đoạn mô tả keybinding editor) ghi lại `Ctrl+K`/`Ctrl+L`.
- ~~**Phím mở `$EDITOR`/vim/nvim thật cho buffer hiện tại**~~ — thử rồi bỏ (2026-08-15). Đã build đầy đủ và verify tay qua tmux (`Ctrl+X` → `Action::EditExternally` → `main.rs` tạm thoát TUI, mở `$EDITOR`/`vi`, đọc lại file khi thoát 0, giữ nguyên buffer khi thoát khác 0) — hoạt động đúng như thiết kế, nhưng user thử xong thấy **trải nghiệm không tốt** và yêu cầu bỏ hẳn, quay lại làm "Editor còn thiếu so với vim thật" bên dưới cho editor tự viết thay vì lối tắt này. Đã revert sạch: `Action::EditExternally`, `Component::editor_text`/`set_editor_text`, `Command::OpenExternalEditor` (context `query-screen`), `RootComponent::active_editor_text`/`set_active_editor_text`, hàm `edit_externally()` trong `main.rs`. **Đừng tự đề xuất lại hướng shell-out `$EDITOR` này nữa nếu chưa có yêu cầu cụ thể mới** — đã thử một lần, user không thích trải nghiệm.

**Editor còn thiếu so với vim thật** (biết trước khi tự viết editor, cố ý bỏ qua ở vòng đầu — xem mục 5 phần "Bỏ `edtui`")

- ~~Undo/redo.~~ Xong (2026-08-15). `u` undo, **`U`** redo — không phải `Ctrl-r` chuẩn vim, vì `Ctrl-r` đã là "mở query history" ở `Context::QueryScreen` và bị chặn ở tầng đó trước khi bao giờ tới được editor (`QueryScreenComponent::handle_key_event` luôn đưa `Context::QueryScreen` vào danh sách context bất kể focus đang ở đâu). `u`/`U` không đụng gì có sẵn: `vim_list::recognize` chỉ nhận `Ctrl-u` (half-page-up), không nhận `u` trần. Checkpoint lưu lại **một lần mỗi lệnh rời rạc**, không phải mỗi ký tự: bấm vào lúc vào Insert mode (`i`/`a`/`I`/`A`/`o`/`O`), trước `x`, trước `dd`, và trong `insert_at_cursor`/`replace_word_before_cursor` (chèn tên từ navigator, load từ history, nhận completion) — nên gõ cả câu rồi `Esc` thì `u` một phát lùi về trạng thái trước khi gõ, đúng hành vi vim thật (không phải xoá từng ký tự). `set_text` (mở file, chọn history, restore session) **xoá sạch undo history** vì buffer mới không liên quan gì tới buffer cũ — không thì `u` có thể hồi sinh lại query đã bị thay thế. Một edit mới sau khi undo thì xoá redo stack (nhánh cũ bị bỏ, đúng quy tắc undo/redo chuẩn). Cap 500 bước (`MAX_UNDO_HISTORY`) để không phình bộ nhớ vô hạn. Verify tay qua tmux: gõ cả câu SELECT rồi Esc → `u` xoá sạch → `U` khôi phục; `x` xoá 1 ký tự rồi `u` khôi phục; `dd` xoá cả dòng rồi `u` khôi phục dòng.
- ~~Visual mode, copy/paste nội bộ (yank/put), search trong buffer (`/`, `n`/`N`).~~ Xong (2026-08-15) — trừ **replace** (`:s/pat/repl/`), chốt tách ra làm sau vì chưa có tiền lệ UI dạng dòng lệnh `:` nào trong app, cần scope riêng. Chốt qua `AskUserQuestion`: Visual áp dụng cả `y`/`d`/`x`/`c` (không chỉ y/d) ngay từ đầu.
  - **Register**: `enum Register { Charwise(Vec<char>), Linewise(Vec<Vec<char>>) }`, một register duy nhất (không đặt tên nhiều register kiểu `"a` như vim thật — quá rộng so với yêu cầu). `dd`/`x` giờ nạp vào register thay vì bỏ đi; `yy` mới (copy dòng, không xoá, không checkpoint); `p`/`P` dán register tại con trỏ (linewise thành dòng mới trên/dưới, charwise chèn inline, tách theo `\n` nhúng trong register nếu selection charwise từng trải nhiều dòng).
  - **Visual mode**: `EditorMode` thêm `Visual` (`v`, charwise)/`VisualLine` (`V`, linewise), field `visual_anchor`. Tách `handle_motion_key` (h/l/0/$ + j/k/gg/G/Ctrl-d/Ctrl-u qua `vim_list`) dùng chung giữa Normal và Visual, để di chuyển trong Visual vẫn tôn trọng fold qua đúng `move_row_to`. Selection **inclusive cả hai đầu** (đúng ngữ nghĩa vim, không phải exclusive kiểu editor thường). `y`/`d`/`x` thao tác xong quay về Normal; `c` xoá xong vào thẳng Insert mode tại điểm cắt (đúng hành vi vim thật). Xoá charwise xuyên nhiều dòng nối đúng phần đầu+cuối còn lại. Render thêm lớp nền `theme().selection_bg` (tái dùng token đã có cho list selection) đè dưới `Modifier::REVERSED` của con trỏ trong `render_line`.
  - **Search**: `QueryEditorComponent::find(pattern, backwards) -> bool` (case-insensitive ASCII, wrap quanh buffer theo đúng `wrapscan` mặc định của vim thật). `/` **không** gắn được vào `Context::QueryScreen` (sẽ đè lên `/` → filter kết quả của `Context::Results` khi đang ở đó) — thêm `Context::Editor` mới (chỉ đưa vào contexts khi `Focus::Editor`, cùng cách `Context::Browse` đã tách theo panel), `n`/`N` cũng ở context đó. Field riêng `buffer_search`/`search_origin`/`last_search` trên `QueryScreenComponent` — **không** tái dùng field `search` (đang phục vụ filter kết quả, ngữ nghĩa Enter/Esc khác hẳn). Hành vi **incremental** giống filter kết quả đã có (nhất quán UX toàn app): gõ tới đâu preview-nhảy tới đó tính lại từ `search_origin` mỗi lần (không phải từ match trước), `Enter` chốt + lưu `last_search`, `Esc` trả cursor về đúng chỗ cũ. Chỉ hoạt động khi `query_editor.mode == Normal` (Insert mode thì `/` đã là ký tự gõ thường qua passthrough có sẵn; Visual mode cố tình không hỗ trợ search-as-motion như vim thật — để dành nếu cần sau).
  - Test: 20 test mới trong `query_editor.rs` (register, Visual charwise/linewise đủ 4 thao tác, undo cho cả paste lẫn Visual delete, di chuyển trong Visual tôn trọng fold, `find` đủ case) + 5 test wiring trong `query_screen.rs` (incremental preview, Esc trả đúng vị trí, Enter+`n` lặp đúng hướng có wrap, `/` ở Focus::Results không đụng buffer search, `/` trong Insert mode vẫn là ký tự thường). Verify tay qua tmux: gõ query thật, `v`+di chuyển thấy nền tô đúng màu `selection_bg`, `y` rồi `p` dán đúng chỗ, `/pattern` thấy thanh `/pattern` hiện đúng vị trí (giữa editor và Results) và cursor nhảy đúng match, `Enter` đóng thanh giữ nguyên vị trí.

**Schema đọc được sâu hơn** (nối tiếp mục 2 "Schema sidebar xem được cột" — panel đó nay là navigator)

- ~~**Elasticsearch**: lấy field từ `GET /_mapping`~~ — xong (2026-08-14). `list_schema` gọi thêm `GET /_mapping` (một request cho mọi index) và gắn field vào từng index. Field lồng nhau được làm phẳng thành đường dẫn có dấu chấm (`customer.address.city`) đúng như cách viết trong query; node kiểu object không tự nó là field; multi-field (`title.keyword`) bị bỏ qua vì đó là chi tiết đánh index chứ không phải field của document. `_mapping` lỗi thì vẫn giữ nguyên danh sách index thay vì làm hỏng cả việc duyệt schema.
- ~~**MongoDB**: suy ra tên field bằng cách đọc mẫu một document mỗi collection~~ — xong (2026-08-15). Chọn hướng **ăn ngay lúc connect** (giống Elasticsearch/SQLite), không làm lazy: `list_schema()` gọi `find_one({})` cho từng collection, N collection = N round trip — chấp nhận được với số lượng collection bình thường, cùng đánh đổi mà SQLite đã chấp nhận với `PRAGMA` mỗi bảng (ghi rõ lý do ngay trong doc comment ở `list_schema` để không ai thắc mắc lại). Làm lazy (chỉ đọc khi user bung node collection trong navigator) cần thêm một hook "node vừa được mở" xuyên qua ranh giới navigator ↔ screen mà kiến trúc hiện tại chưa có (outline() hiện chỉ đọc, không có sự kiện) — để dành nếu sau này thấy connect chậm thật. Field lồng nhau làm phẳng thành đường dẫn có dấu chấm bằng `flatten_document`, cùng ý tưởng với `flatten_properties` của Elasticsearch; mảng báo là một field kiểu `Array` chứ không mở từng phần tử, vì các phần tử của một mảng trong document không đảm bảo cùng shape để mà báo một type chung. Tên kiểu lấy từ `Bson::element_type()` debug ra thẳng (`Int32`, `ObjectId`, `String`...) — passthrough như mọi driver khác, không tự đặt tên chuẩn hoá riêng. `_id` được đánh dấu `primary_key` (mỗi document Mongo có đúng một field đó, gần nhất với khái niệm khoá chính) — chưa dùng cho gì (grid vẫn read-only cho Mongo, `edit_sql`/`edit_source` mặc định `None`) nhưng đã sẵn sàng nếu sau này build edit-row cho Mongo. Collection rỗng hoặc sample lỗi thì báo `columns: []` chứ không làm hỏng cả list_schema cho collection khác. Verify tay qua tmux: insert một document lồng `address: {city, zip}` + mảng `tags` vào Mongo thật trong Docker, mở navigator → bung collection → thấy đúng `_id ObjectId pk`, `name String`, `age Int32`, `address.city String`, `address.zip String`, `tags Array`.
- Cả hai đều làm autocomplete tốt lên ngay, vì nguồn gợi ý lấy thẳng từ `SchemaInfo`.

**Chưa được scope, đã nêu ở mục 5 cũ**

- Nhiều theme preset dựng sẵn (hiện: một theme dark + override từng màu).
- Cho phép remap cả phím vim *bên trong* editor (hiện cố định theo vim chuẩn — xem ghi chú phạm vi ở đầu `crates/tradar-core/src/keymap.rs`).
- Cột trong bảng kết quả resize được bằng tay (hiện tự tính theo giá trị rộng nhất, cap 40 ký tự).

**Kỹ thuật, không ảnh hưởng người dùng ngay**

- ~~**Timeout connect chỉ có ở Postgres**~~ — xong (2026-08-15). Thêm `tradar_connector_api::CONNECT_TIMEOUT` (5s) + `with_connect_timeout(target, future)`; cả **năm** `Connector::connect` bọc qua nó. Đặt ở `tradar-connector-api` chứ không copy vào từng connector vì các client bên dưới bất đồng hoàn toàn về hành vi khi host không trả lời (sqlx có timeout riêng, `redis`/`mongodb` có default riêng, `reqwest` không có gì) — gom một chỗ thì mọi backend có cùng giới hạn và cùng câu thông báo. Postgres vẫn giữ `acquire_timeout` trên pool (giờ lấy từ hằng số chung) vì nó còn chặn cả các lần acquire sau này lúc đang chạy query, không chỉ lần connect đầu. Test: 3 unit test trong `tradar-connector-api` dùng `#[tokio::test(start_paused = true)]` + `std::future::pending()` nên xác nhận được giới hạn mà không tốn 5s thật. Verify tay: Elasticsearch trỏ vào IP blackhole (`10.255.255.1`) — trước đây `reqwest` treo tới lúc OS bỏ cuộc, giờ báo `connecting to 'http://10.255.255.1:9200' timed out after 5s` đúng ở giây thứ 5.
- **`Component: Send`** — xem "Đã cân nhắc và gác lại" trong `docs/architecture.md`; lý do gốc (`edtui` giữ `Rc`) đã biến mất nhưng chưa ai verify lại.

## Mở rộng UI theo mockup "Tradar TUI" (bắt đầu 2026-08-15) — 7/7 xong

Nguồn: file thiết kế `claude.ai/design` project "Còn nhớ Tradar" (13 màn hình, review 2026-08-15, ghi lại ở mục "Chưa làm, chưa được scope" trước khi có mục riêng này). User xác nhận muốn xây theo hướng đó và scope thêm nữa. **Thứ tự ưu tiên user chốt 2026-08-15:** việc nhỏ, đụng ít chỗ trước; connector mới (mỗi cái một crate, tốn thời gian nhất) làm sau cùng.

1. ~~**Code folding trong SQL editor.**~~ Xong (2026-08-15). `za` (đúng phím vim thật, dù editor không có `zo`/`zc`/`zR`/`zM` khác) gấp/mở câu SQL nhiều dòng đang chứa con trỏ. Đơn vị gấp là **câu lệnh**, không phải block tuỳ ý — tái dùng thẳng `query_driver::split_sql_statements` (bộ tách câu đã có sẵn cho `Ctrl+A`/F5) để tính line range của từng câu qua `foldable_ranges()`, nên không cần dựng thêm khái niệm "region" nào mới; câu một dòng không có gì để gấp nên bị lọc ra. Chỉ áp dụng khi `Dialect::Sql` — Mongo/Redis/Elasticsearch mỗi câu vốn một dòng, `foldable_ranges()` trả rỗng cho các dialect khác, không hiện gutter luôn (không tốn 2 cột cho tính năng không bao giờ chạy). Trạng thái gấp lưu trong `folded: HashSet<usize>` **keyed theo line index của đầu câu**, không phải theo identity câu — cố ý chấp nhận: sửa buffer phía trên một fold có thể làm entry hết khớp với line range tính lại (tự "tuột" fold một cách im lặng thay vì gấp nhầm chỗ khác), fold lại bằng `za` là đủ, không đáng xây cơ chế theo dõi phức tạp hơn cho một tính năng hiển thị. Ba điểm hành vi quan trọng, đều có test: (1) `j`/`k`/`gg`/`G`/`Ctrl-d`/`Ctrl-u` xuyên qua `move_row_to` — hàm chốt duy nhất mọi di chuyển hàng đi qua — coi cả cụm đã gấp như một dòng: đi xuống vào vùng ẩn nhảy thẳng tới dòng ngay sau fold, đi lên nhảy về dòng đầu fold, cursor không bao giờ đứng được ở một dòng đang ẩn; (2) `checkpoint()` (gọi trước mọi lệnh sửa buffer: `i`/`a`/`o`/`O`/`x`/`dd`/chèn tên từ navigator/nhận completion) tự mở fold đang chứa cursor trước khi sửa — không thì nội dung vừa gõ biến mất ngay dưới fold còn đóng; (3) `set_text` (mở file, chọn history, restore session) xoá sạch `folded` cùng lý do đã xoá undo history: buffer mới không liên quan gì tới fold cũ. Gutter 2 cột (`▸`/`▾` + khoảng trắng) chỉ vẽ khi `Dialect::Sql`; dòng đầu câu đang gấp có thêm text `⋯ N lines folded`. `cursor_screen_position`/`scroll_into_view` đổi từ tính theo raw line index sang tính theo **vị trí trong danh sách dòng đang hiển thị** (bỏ dòng ẩn) — cần cho cả việc cuộn màn hình đúng lẫn neo popup autocomplete đúng chỗ khi có fold phía trên con trỏ. Verify tay qua tmux: gõ 2 câu SQL (câu 1 ba dòng, câu 2 một dòng) vào SQLite connection thật, `za` ở câu 1 → gutter `▸ SELECT id, name ⋯ 2 lines folded`, hai dòng `FROM`/`WHERE` biến mất, câu 2 vẫn đúng vị trí; `j` từ dòng gấp nhảy thẳng qua câu 2 (xác nhận qua vị trí con trỏ ở mode bar, `4:9` thay vì `2:1`); `za` lại → mở về đủ 4 dòng.
2. ~~**Ô kết quả kiểu object/array mở rộng được tại chỗ.**~~ Xong (2026-08-15), thu hẹp phạm vi so với mô tả ban đầu — chi tiết lý do bên dưới. `Space` (context `results`, `Command::TogglePreview` mới trong `tradar_core::keymap`) bật/tắt một panel cố định cao `PREVIEW_HEIGHT` (8 dòng, co lại nếu panel Results không đủ chỗ) phía dưới bảng, hiện giá trị đầy đủ của cell đang chọn — pretty-print qua `serde_json::to_string_pretty` nếu giá trị đó parse được thành JSON object/array (cột jsonb, text JSON của Postgres/SQLite), giữ nguyên văn nếu không. **Chỉ áp dụng cho `QueryResult::Table`** (Postgres/SQLite, và Cassandra sau này) — không phải cả 3 dạng đã nêu lúc scope: `QueryResult::Documents` (Mongo, và Elasticsearch — cả hai đều trả `Documents` chứ không phải `Table`, xem `query_driver.rs`) đã tự pretty-print toàn bộ document nhiều dòng ngay trong danh sách kết quả từ trước (`serde_json::to_string_pretty` trong nhánh `Documents` của `draw()`), nên không có gì "bẹt" cần mở rộng ở đó — phát hiện này lúc đọc lại code trước khi làm, không phải lúc đang làm dở. Panel tự đóng khi cursor đổi cell (`apply_move`/`prev_column`/`next_column`/`click`) hoặc có result/error mới — mở panel rồi di chuyển mà không đóng sẽ hiện giá trị của cell cũ mà không nói rõ. Verify tay qua tmux: SQLite thật, cột text chứa `{"theme":"dark","flags":["vip","early-adopter"]}` (bị cắt còn 40 ký tự trong grid), `Space` mở panel `metadata — full value` hiện đúng JSON pretty-print nhiều dòng; `Space` lần nữa đóng; di chuyển cell (`h`) tự đóng panel đang mở.
3. ~~**Transaction control (auto-commit ON/OFF, commit F8, rollback F9).**~~ Xong (2026-08-15). Thu hẹp phạm vi khỏi mockup theo hướng thực dụng hơn: không có UI toggle "auto-commit ON/OFF" riêng — trạng thái đó **suy ra thẳng** từ việc có transaction đang mở hay không (giống hệt cách `psql` tự báo), không phải một mode phải bật/tắt tách biệt. Người dùng gõ và chạy `BEGIN`/`COMMIT`/`ROLLBACK` như SQL chuẩn (giữ đúng triết lý "không bịa ngôn ngữ query riêng" của CLAUDE.md) — `query_driver::transaction_control()` (heuristic theo từ khoá đầu câu, cùng kiểu với `returns_rows`) nhận diện 3 lệnh này và route sang xử lý transaction thật của driver thay vì gửi văn bản đó xuống DB, vì shape pool-mỗi-câu-một-connection hiện tại của `execute()` không thể tự nhiên "hiểu" một `BEGIN` đứng riêng — câu tiếp theo rất có thể rơi vào một connection pool khác. `QueryDriver` có thêm đúng 1 method mới, `in_transaction() -> bool` (mặc định `false`, cùng pattern với `ping()`) — không có `begin_transaction()`/`commit_transaction()` ở tầng trait, vì Postgres/SQLite tự xử lý nội bộ qua `execute()`, tầng trait không cần biết. Cả hai connector SQL thêm `transaction: tokio::sync::Mutex<Option<sqlx::Transaction<'static, DB>>>` + `AtomicBool` (đọc nhanh không cần lock, vì UI gọi `in_transaction()` mỗi frame) — khi có transaction đang giữ, mọi statement khác chạy qua `&mut *tx` thay vì thẳng qua pool, nên các câu trong cùng transaction thấy được thay đổi chưa commit của nhau và (đã verify) một connection khác thì không. `F8`/`F9` (context `query-screen`) chỉ gọi `engine.submit_query("COMMIT"/"ROLLBACK")` — tái dùng nguyên pipeline chạy query có sẵn, không thêm cơ chế async mới — nhưng **cố tình không đụng `last_query`** (field quyết định bảng nào grid đang sửa được), nếu không F8/F9 sẽ làm hỏng tính năng edit-cell của kết quả đang hiển thị. Badge `transaction open (F8 commit / F9 rollback)` ở mode-bar của editor, im lặng khi auto-commit (giống badge disconnected: chỉ lên tiếng khi cần chú ý). Bug phát hiện lúc verify tay lần đầu (tưởng là do code, hoá ra không phải): gửi lệnh tmux quá dồn dập (không đợi tick hoàn tất) làm 2 lần chạy F5 liên tiếp trông như "bị treo ở kết quả cũ" — viết thêm test tái hiện đúng path `QueryEngine` thật (`a_transaction_through_query_engine_does_not_get_stuck_pending` trong `tradar-sqlite`) để loại trừ, test pass ngay, xác nhận đó là lỗi thao tác chứ không phải bug; verify lại chậm rãi qua tmux thì đúng như mong đợi. Test: unit test cho `transaction_control()`; integration test cho SQLite (đọc/ghi trực tiếp qua `driver.execute`, không cần Docker) và Postgres (testcontainers) đều xác nhận isolation thật — insert trong transaction chưa commit thì một connection độc lập khác không thấy được. Verify tay qua tmux với SQLite thật: `BEGIN` → badge hiện → `INSERT` → `F8` → badge biến mất, `SELECT` từ đầu thấy đúng row; lặp lại với `F9` → row không tồn tại.
4. ~~**Redis: key browser theo type (hash/string/list/set/zset) thay vì console lệnh thô.**~~ Xong (2026-08-15). Shape chốt qua `AskUserQuestion` trước khi code (4 quyết định UX: sidebar là panel riêng trong tab Redis chứ không mở rộng navigator; chọn key thay hẳn panel results; mặc định mở connection Redis vào browse mode, phím riêng bật console; làm cả 5 type cùng lúc). Chi tiết kỹ thuật đầy đủ ở `docs/architecture.md` mục Redis → "Key browser". Tóm tắt: `QueryScreenComponent` thêm `mode: ScreenMode{Browse,Console}` + sidebar `browse: Option<BrowseSidebarComponent>` (`Some` chỉ khi `connection.driver == "redis"`, cùng cơ chế match-driver-id đã dùng cho `Dialect::Sql` — không thêm `Capability` mới); `Ctrl+G` (`Command::ToggleBrowseMode`) chuyển mode, `Enter` trên sidebar (`Context::Browse`, `Command::BrowseOpen`) gọi `QueryEngine::submit_browse` mới (song song `submit_query`, không push history) → `QueryDriver::browse_entry` mới (mặc định unsupported, chỉ RedisDriver override). RedisDriver tra `SchemaInfo.kind` (field mới, `TYPE` mỗi key — `list_schema` giờ loop `SCAN` tới cursor 0 thay vì chỉ batch 100 đầu) để chọn đúng lệnh Redis, chạy qua `execute()` có sẵn (console mode không đổi hành vi/test), rồi reshape JSON thành `QueryResult::Table` — không cần component render riêng, `ResultsComponent` đã biết vẽ `Table` từ trước. Console mode giữ nguyên 100%, không tách Redis thành Session/Screen riêng kiểu Kafka. Test: unit cho `submit_browse`/toggle/sidebar movement, integration (testcontainers) cho `list_schema` (pagination qua 100 key + type mỗi key) và `browse_entry` cả 5 type + 1 type không hỗ trợ báo lỗi rõ ràng.
5. ~~**Connector Kafka.**~~ Xong (2026-08-16). Mockup Screen 7 (topic list, consumer group, tail message, offset/partition) — v1 chỉ làm **Topics mode** (tail + publish), **Groups mode (lag theo consumer group) hoãn**, xem mục 9 bên dưới. Connector phi-query đầu tiên trong repo, không dùng `tradar-query-workbench` — tự viết `KafkaSession`/`KafkaScreen` (`crates/connectors/tradar-kafka`) đúng theo `Connector`/`Session`/`Screen` đã đặc tả trong `docs/architecture.md` mục "Kiến trúc mục tiêu: connector pluggable", và đúng shape đã chốt ở mục con "Thiết kế UI: Kafka và RabbitMQ" cùng file. Tóm tắt kỹ thuật: `target` là bootstrap servers trần (`host:port`, giống Cassandra dùng contact point trần); sidebar topic list (lọc bỏ topic nội bộ `__*`) + bảng message đang tail (partition/offset/key/value), `Enter` tail từ latest, `b` từ earliest, `Space` pause/resume (đóng băng view ở độ dài buffer lúc pause, `KafkaSession` vẫn nhận ngầm, buffer cap 500 message gần nhất), `r` refresh topic list, `p` mở compose panel publish (`Enter` gửi ngay, không `y`-confirm). Tail thật qua 1 `StreamConsumer` (`rdkafka`) chạy trong task nền riêng mỗi lần tail, `group.id` ngẫu nhiên/ephemeral để không đụng consumer group thật nào; `tick()` rút event có giới hạn (64/lần) — đây là firehose thật đầu tiên khai thác đúng cơ chế bounded-drain đã đặc tả sẵn trong kiến trúc. Client library: `rdkafka` (feature `cmake-build`, tự build `librdkafka` từ vendored source) — phá tiền lệ pure-Rust của Cassandra vì `kafka-protocol` (pure Rust) đòi tự dựng lại consumer-group protocol, không tương xứng công sức cho v1. Test: unit cho descriptor/ephemeral-group-id/compose-field-cycle; integration (Docker, `testcontainers::GenericImage` mô phỏng đúng cấu hình KRaft single-node của `docker-compose.yml`, port 9092 cố định — lý do giống Cassandra) cho round-trip tail + publish + list_topics thật. Verify tay qua tmux: `docker compose up kafka`, tạo topic ngoài app rồi `r` refresh thấy ngay, tail 1 topic, publish message từ ngoài app qua `kafka-console-producer` trong lúc đang tail — message tự xuất hiện không cần thao tác gì (xác nhận tail thật, không phải poll giả); publish từ trong app qua compose panel; pause giữ nguyên view trong lúc publish message khác, resume hiện lại đúng.
6. ~~**Connector RabbitMQ.**~~ Xong (2026-08-16). Cùng nhóm non-query với Kafka ở trên — chưa có mockup screen riêng trong file thiết kế gốc (chỉ xuất hiện trong danh sách connection ở Screen 1), tự vẽ shape UI ở mục "Thiết kế UI: Kafka và RabbitMQ" trong `docs/architecture.md`. Làm trước Kafka trong cùng đợt vì rủi ro thấp hơn (không cần task consumer nền, không cần chọn client library mới — dùng thẳng `reqwest` đã có sẵn nhờ driver Elasticsearch) để verify shape `Session`/`Screen` phi-query hoạt động đúng trước khi làm Kafka. Tóm tắt kỹ thuật: `target` là URL đầy đủ tới Management API (`http://user:pass@host:15672/vhost`, vhost mặc định `%2f`), parse bằng `reqwest::Url` — không thêm crate `url`. Sidebar 2 mode (`Ctrl+G` toggle): **Queues** (tên + ready/unacked/consumer count, `Enter` peek N message gần nhất không phá huỷ qua `ackmode: ack_requeue_true`) và **Exchanges** (tên + type, `Enter` xem danh sách binding). `r` refresh, `p` publish (chọn exchange + routing key + payload qua compose panel 3 field). Quyết định kỹ thuật chốt: dùng **Management HTTP API** cho mọi thao tác thay vì AMQP client — nghĩa là poll theo yêu cầu (`r`), không tail real-time như Kafka (`Capability` không có `Streaming`/`Tail`); không tạo/xoá queue/exchange/vhost (chỉ browse + publish). Test: unit cho parse `target`/JSON response/descriptor; integration (Docker, `testcontainers::GenericImage`, port 5672+15672 cố định) cho round-trip publish + peek + refresh_queues thật. Verify tay qua tmux: `docker compose up rabbitmq`, tạo queue ngoài app rồi `r` refresh thấy ngay, publish từ trong app qua compose panel, peek lại thấy đúng payload (không mất khỏi queue), toggle Queues/Exchanges bằng `Ctrl+G`.
**Hai điều phát hiện lúc verify integration test cho Kafka/RabbitMQ (không phải bug code — hành vi thật của toolchain/broker cần biết trước):**
1. **`rdkafka`'s `cmake-build` cần header `curl/curl.h` dù cấu hình `WITH_CURL=0`.** Build lỗi ngay bước đầu tiên (`fatal error: curl/curl.h: No such file or directory`) trên máy dev mặc định — thiếu gói hệ thống `libcurl4-openssl-dev`, không phải lỗi cấu hình Cargo/CMake của connector. Đã cài thêm gói đó rồi build lại thành công; không có cách nào tắt hẳn phụ thuộc curl của `librdkafka` phiên bản này qua CMake flag.
2. **RabbitMQ 4.x deprecate queue non-durable/non-exclusive theo mặc định.** `PUT /api/queues/{vhost}/{queue}` với `"durable": false` bị 400 kèm lỗi `transient_nonexcl_queues is deprecated`. Không ảnh hưởng connector (chỉ browse/peek/publish, không tự tạo queue), chỉ ảnh hưởng test helper tạo queue thử — sửa bằng `"durable": true`.

7. ~~**Connector Cassandra.**~~ Xong (2026-08-15). Mockup Screen 5 (CQL, `metap_ks.events`). Khác Kafka/RabbitMQ: Cassandra khớp shape query — tái dùng `tradar-query-workbench` nguyên vẹn giống 5 connector hiện có, không cần Screen tự viết riêng. Chọn `scylla` (không phải `cassandra-cpp` — thuần Rust, không cần biên dịch C/cmake). Chi tiết kỹ thuật đầy đủ ở `docs/architecture.md` mục "Phạm vi ngôn ngữ query theo từng driver" → bullet **Cassandra**. Tóm tắt: connector thứ 6 (`crates/connectors/tradar-cassandra`), `target` là contact point trần `host:port` (không phải URI), `list_schema` duyệt hết mọi keyspace không phải hệ thống và đặt tên `<keyspace>.<table>`, cột đánh dấu `primary_key` khi `kind` là `partition_key`/`clustering`, `execute()` cho câu ghi luôn báo `Affected { rows: 0 }` (CQL không có affected-row count, không phải rút gọn), không có transaction control (giữ default như Mongo/ES/Redis), chưa có `edit_sql`/`edit_source` (để dành, không bịa cú pháp `WHERE` cho CQL). `testcontainers-modules` không có feature `cassandra` nên test dùng `testcontainers::GenericImage` tự viết wait-strategy + nới `startup_timeout` lên 150s (ảnh JVM khởi động chậm, vượt mặc định 60s của crate). Test: unit cho `stringify_cql_value` (từng biến thể `CqlValue`, fallback `Debug` cho loại phức tạp), `crud_snippet` delegate, `in_transaction` luôn `false`; integration (Docker, `testcontainers::GenericImage`) cho round-trip đầy đủ — tạo keyspace/table composite key thật, xác nhận `list_schema` đánh đúng partition+clustering key theo thứ tự, `execute` INSERT/SELECT ra đúng shape. Verify tay qua tmux: connect vào Cassandra thật (`docker compose up cassandra`), tạo keyspace/table qua editor, navigator hiện đúng `demo.events`, CRUD snippet từ navigator ra đúng cú pháp CQL nhiều dòng.

**Hai bug thật phát hiện lúc verify integration test (không phải lỗi Docker tạm thời — đã root-cause bằng `cqlsh` trực tiếp), cả hai đã sửa:**
1. **`broadcast_rpc_address` trỏ vào IP nội bộ Docker bridge, không route được từ ngoài container.** `connect()` (control connection, dùng thẳng contact point đã cho) vẫn thành công, nhưng driver `scylla` mở một pool connection *riêng* cho mỗi query thật, tới địa chỉ Cassandra tự quảng bá qua `system.local`/`system.peers` — mặc định đó là IP bridge nội bộ (`172.x.x.x`), nên `connect()` OK nhưng `ping()`/mọi query sau đó đều timeout, trông giống hệt "vừa mới start chưa kịp warm up" (đã tưởng nhầm vậy lúc đầu, tốn thời gian debug retry-loop trước khi cqlsh chỉ thẳng ra field `broadcast_rpc_address` trong `system.local`). Sửa bằng env `CASSANDRA_BROADCAST_RPC_ADDRESS: 127.0.0.1` (image chính thức của Cassandra đọc biến này) ở cả `docker-compose.yml` lẫn container test (`with_env_var` trên `GenericImage`) — chỉ đúng cho single-node dev/test, không phải fix chung cho cluster nhiều node thật. Vì Cassandra luôn tự quảng bá đúng port nội bộ (9042, không cấu hình riêng được), container test còn phải ép `with_mapped_port(9042, 9042.tcp())` thay vì để Docker cấp port ngẫu nhiên — nghĩa là test cần port 9042 trống, không chạy song song được với `docker compose up cassandra` (đã ghi chú ngay trong test).
2. **`list_schema` không sắp đúng thứ tự cột trong composite key.** `SELECT ... FROM system_schema.columns` không đảm bảo thứ tự dòng trả về, nên `PRIMARY KEY (user_id, group_id)` có lúc ra `[group_id, user_id]`. Sửa bằng cách lấy thêm cột `position` (0-based, tính riêng trong từng `kind`) và sort theo `(kind_rank, position)` — `partition_key` trước `clustering` trước phần còn lại, đúng thứ tự khai báo trong từng nhóm.

8. **Kafka: mode Groups (lag theo consumer group).** Hoãn khỏi v1 của mục 5 ở trên (Topics mode + publish đã xong, 2026-08-16) — mockup Screen 7 có phần consumer group nhưng lấy lag đúng nghĩa (current offset của 1 group cụ thể theo từng partition, so với high-water mark) với `rdkafka` cần dựng 1 consumer tạm gán `group.id` được chọn rồi gọi `committed()`, phức tạp hơn đáng kể so với phần Topics đã làm. Đọc-only, không seek/reset offset (giữ nguyên non-goal đã ghi trong `docs/architecture.md`).

Không đụng: autocomplete (Screen 2, đã xong 2026-08-14) và Mongo query bar dùng method chaining trong mockup (`docs/architecture.md` mục MongoDB đã ghi rõ giới hạn "không chaining" — mockup chỉ chưa khớp với giới hạn đã biết, không phải gap mới).

**Rà lại mockup lần nữa (2026-08-15)** — user hỏi lại "hiện đang thiếu gì", đọc kỹ text từng màn hình trong file thiết kế (qua artifact xuất, không phải 7 mục đã liệt kê ở trên) thấy thêm mấy điểm chưa từng nêu, **chưa scope, chưa ai bắt tay vào**:

- ~~**Navigator không có ô `/` lọc cây**~~ — xong (2026-08-16). `/` (`Context::Navigator`, tái dùng `Command::Search` đã có cho Results chứ không thêm command mới) mở một thanh nhập một dòng dưới cây, cùng idiom `Esc` huỷ hẳn / `Enter` giữ lại đã dùng cho filter kết quả và buffer-search. `NavigatorComponent` tự giữ `filter: String` + `filter_input: Option<TextInput>` (không đẩy lên `RootComponent` dù nó điều phối phím — filter là cách `rows()` tự thu hẹp, không phải state của host). Lọc không phân biệt hoa thường, khớp tên connection hoặc label/detail của entry; **một connection còn hiện dù tên nó không khớp**, miễn có ít nhất một entry con khớp — lọc theo tên bảng mà làm mất luôn dòng connection thì không còn gì để click/mở. Không tự bung node đang đóng để lộ ra một match ẩn bên trong (giữ đúng tinh thần "lọc cái đang mở", giống filter kết quả không tự jump-to-match). `RootComponent::handle_navigator_key` chặn mọi phím ở đầu hàm khi `navigator.is_filtering()`, route thẳng vào `filter_key_event` trước khi qua `keymap().resolve_in` — cùng lý do query screen làm vậy với search bar của nó: chữ cái gõ vào ô lọc không được hiểu thành lệnh (`j`, `g`...). Test: unit trong `navigator.rs` (lọc theo tên connection, lọc theo tên bảng vẫn giữ connection row, Esc xoá hẳn filter, Enter giữ lại, mở lại prefill đúng filter cũ) + integration trong `components/mod.rs` (gõ `/postgres` rồi Enter, xác nhận `enter` tiếp theo nhắm đúng connection đã lọc chứ không phải connection đang chọn trước khi lọc).
- **Navigator phẳng hơn mockup** — hiện đi thẳng connection → table → column (`query_screen.rs::outline()`); mockup có thêm cấp schema (`public`) và nhóm theo loại object (`Tables`/`Views`/`Functions`) dưới mỗi schema. Cũng không có danh sách "query gần đây" gắn thẳng vào cây navigator như mockup (`sql users.sql`, `redis session:*`...) — hiện recent-files chỉ vào được qua `Ctrl+O`. **Rà lại 2026-08-16, cố tình để nguyên**: đây là phần duy nhất còn lại trong đợt rà mockup lần 2 (5/6 mục còn lại đã xong, xem các mục ~~gạch ngang~~ quanh đây) — hỏi lại trước khi làm vì thêm cấp schema **đảo ngược một quyết định đã chốt** lúc xây navigator ban đầu (mục 6, "Bỏ cấp `database` giữa" — chính vì thêm cấp đó cần method "liệt kê database" mới trên `QueryDriver`, phải implement cho cả 6 connector dạng query trong khi Elasticsearch/Mongo/Redis còn không có khái niệm này, cây sẽ không đồng nhất giữa các connector). User chọn dừng ở 5/6, giữ nguyên navigator 2 cấp hiện tại, để dành việc này cho tới khi có yêu cầu cụ thể mới — **đừng tự thêm cấp schema/nhóm object/recent-queries-trong-cây nếu chưa có chỉ đạo rõ ràng**.
- ~~**Bảng kết quả không hiện kiểu cột**~~ — xong (2026-08-16). **Không** đổi shape `QueryResult::Table` (`columns: Vec<String>` giữ nguyên — đổi kiểu phần tử sẽ đụng khoảng 60 chỗ dựng/so khớp `QueryResult::Table` khắp workspace, phần lớn là test literal, cho một tính năng chỉ ảnh hưởng hiển thị). Thay vào đó: hàm thuần mới `query_driver::column_types(columns, schema, table)` tra kiểu từng cột theo tên trong `SchemaInfo` của `table` (không tìm thấy bảng, không có bảng nguồn, hay cột không có trong schema — kể cả 3 trường hợp đều trả `None` cho cột đó, không đoán). `QueryScreenComponent::column_types()` gọi hàm này mỗi frame với `self.engine.edit_source(last_query)` làm tên bảng — **tái dùng nguyên** lookup đã có cho row-edit, nên join/nhiều bảng tự động ra `None` hết (đúng lý do `edit_source` đã bảo thủ). `ResultsComponent` nhận field mới `column_types: Vec<Option<String>>` qua `set_column_types()` (cùng idiom `draw_running` — screen truyền vào mỗi frame vì chỉ nó với tới được schema). Header hiện `id INTEGER` (tên + kiểu cách nhau một dấu cách, không phải cú pháp `id: INTEGER`), và **độ rộng cột được tính trên cả chuỗi có kiểu** chứ không phải chỉ tên — nếu không phần kiểu sẽ bị cắt bởi `truncate()` để vừa độ rộng tính theo tên trần. Panel xem cell đầy đủ (`Space`) dùng chung nhãn đó nên cũng tự có kiểu trong tiêu đề. View bảng phẳng của `Documents` (Mongo/ES, `t` toggle) **cố tình không có kiểu** — không có schema SQL nào áp dụng cho JSON tuỳ ý, luôn truyền `&[]`. Test: unit cho `column_types` (tra đúng theo tên, cột không có trong schema, không có bảng nguồn, bảng không có trong schema — cả 4 đều `None` đúng chỗ) + `QueryScreenComponent::column_types()` (tra đúng qua `edit_source`, `None` hết với JOIN) + render test trong `results.rs` (header hiện đúng "id INTEGER", cột không có kiểu hiện tên trần chứ không phải "name None", tiêu đề preview panel có kiểu).
- ~~**Không có đồng hồ đếm giờ lúc query đang chạy**~~ — xong (2026-08-16). `QueryEngine` có thêm `running_since: Option<tokio::time::Instant>` (set ở cả 3 chỗ spawn — `submit_query`/`submit_browse`/`submit_all`, clear ở `cancel()` và khi `tick()` nhận outcome khớp epoch), cộng accessor `elapsed_running()`. `ResultsComponent::draw_running` đổi tham số từ `bool` sang `Option<Duration>`, vẽ spinner 10-frame braille (`⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏`, đổi frame mỗi 80ms tính từ elapsed) cộng `{:.1}s elapsed`. **Điểm mấu chốt để spinner thực sự chạy**: `Session::tick()` của `QueryEngine` trước đây chỉ trả `changed = true` khi có outcome/ping mới — không có gì khiến `main.rs` redraw giữa lúc query đang chạy. Thêm `if self.pending { changed = true }` ở cuối `tick()` để `dirty` được set mỗi vòng lặp (~50ms, theo `event::poll` timeout có sẵn) trong lúc chờ, giống hệt cách ping đã lợi dụng chu kỳ tick sẵn có — không thêm cơ chế redraw mới.
- ~~**Lỗi query hiện nguyên văn, không parse**~~ — xong (2026-08-16), chỉ Postgres (SQLite/Mongo/ES/Redis/Cassandra không có DETAIL/HINT/position để mà parse). **Không** đổi shape lỗi xuyên suốt app (`QueryOutcome::Failed { error: String }` giữ nguyên) — thay vào đó Postgres tự làm giàu chuỗi lỗi *trước khi* nó rời khỏi connector, đúng nguyên tắc cô lập connector. `format_pg_error()` (`crates/connectors/tradar-postgres/src/lib.rs`) bắt tại 2 điểm `?` của `run()` (`execute`/`fetch` stream), downcast `sqlx::Error::as_database_error()` → `sqlx::postgres::PgDatabaseError` (có sẵn trong `sqlx-postgres`, không cần thêm dependency), nối `message` gốc với dòng `LINE n: <text>` + `^` chỉ đúng cột (khi server trả `position`, qua hàm thuần `line_and_caret` — tính theo **ký tự**, không phải byte, vì `position` của Postgres là 1-based char index), rồi `DETAIL:`/`HINT:` nếu có. Lỗi không phải `Error::Database` (mất kết nối, pool timeout...) rơi thẳng về `Display` cũ của `sqlx::Error`, không đụng gì. Thêm 3 `Command` mới (`retry-query`/`edit-query`/`copy-error`, phím mặc định `r`/`e`/`c`, `Context::Results` — không đụng phím nào đã có ở context này lẫn `Context::QueryScreen`/`Context::List` gộp chung khi `Focus::Results`), cả ba đều **no-op khi không có lỗi đang hiện** (không chiếm phím vô nghĩa): `r` gọi lại đúng `last_query` đã lưu (không đọc lại vị trí con trỏ — con trỏ có thể đã di chuyển), `e` chuyển focus về editor (text lỗi vẫn còn nguyên trong buffer, không cần làm gì thêm), `c` tái dùng `yank_to_clipboard` (OSC52) đã có sẵn cho `y`. Status bar chỉ hiện gợi ý 3 phím này khi `results.last_error` đang `Some` — không làm rối status bar lúc bình thường. Test: unit cho `line_and_caret` (một dòng, nhiều dòng, ngoài phạm vi, vị trí 0) + integration test thật qua testcontainers (`SELECT * FRO users` ra đúng `LINE 1:`/`^`, insert trùng primary key ra đúng `DETAIL:`) + integration test trong `query_screen.rs` cho `r`/`e` (đếm số lần driver thật sự được gọi để phân biệt "có chạy lại" với "không làm gì", `e` chỉ chuyển focus khi có lỗi). **Chưa làm**: parse `LINE`/`^` xuống thành cell riêng để render caret có màu khác (mockup có tô màu `^`) — hiện toàn bộ message (kể cả `LINE`/`^`/`DETAIL`/`HINT`) là một chuỗi `Paragraph` phẳng, không phải rich text; đủ để đọc, chưa đẹp bằng mockup.
- ~~**Không có phím chuyển Table ⇄ JSON** cho kết quả Mongo/Elasticsearch.~~ Xong (2026-08-16). `t` (`Context::Results`, `Command::ToggleResultView`) chuyển `QueryResult::Documents` giữa JSON pretty-print (mặc định, giữ nguyên hành vi cũ) và một bảng phẳng — cột là hợp mọi key cấp 1 xuất hiện trong cả result, **sắp theo alphabet** (không phải thứ tự xuất hiện — tránh phụ thuộc vào `serde_json::Map` có bật `preserve_order` hay không, một chi tiết cấu hình dependency không nên ảnh hưởng hành vi UI). Document thiếu field hiện ô trống (không phải `"null"` — trống nghĩa là "không có field", null nghĩa là "có field, giá trị null", hai thứ khác nhau); object/array lồng nhau hiện compact JSON một dòng, xem đầy đủ qua panel preview (`Space`) có sẵn — không cần code render riêng. Tái dùng gần như nguyên vẹn hạ tầng bảng đã có cho `QueryResult::Table` (cell cursor, cuộn ngang, filter `/`, yank `y`, preview `Space`) bằng cách tách phần vẽ bảng (`draw_table_body`) thành **hàm rời thay vì method** — `columns`/`rows` mượn từ field `last_result`/`doc_table` của `ResultsComponent` trong khi lời gọi method `&mut self` cần mượn nguyên `self`, hai thứ xung đột qua ranh giới method call dù là 2 field khác nhau; hàm rời nhận từng field cần sửa (`selected_col`, `col_offset`, `visible_height`, `column_spans`, `rows_area`, `table_state`) làm tham số `&mut` riêng để giữ được borrow rời nhau. Không đổi `EditCell`/`DeleteRow` — vẫn bị chặn đúng như trước vì `edit_source` của driver Mongo/ES luôn trả `None`, không liên quan gì đến view đang hiện. Test: unit cho `documents_as_table` (field thiếu, object lồng nhau, thứ tự cột), toggle qua lại, filter/yank trong table view, refresh tính lại bảng. Verify tay qua tmux: Mongo thật, 2 document không cùng field (`{name,age}` và `{name,city,meta:{vip}}`), `t` bật table view thấy đúng cột `_id,age,city,meta,name` sắp alphabet, ô thiếu trống, `meta` hiện compact JSON, `Space` trên ô đó mở panel pretty-print đúng, `/hanoi` lọc đúng 1 dòng, `t` lại quay JSON view giữ nguyên filter.
- ~~**Redis browse mode thiếu TTL + dòng lệnh thực tế**~~ — xong (2026-08-16). `SchemaInfo` có thêm field `ttl: Option<i64>` (giây còn lại; `None` cho backend không có khái niệm TTL, và cũng cho key Redis không đặt expiry — `TTL` trả `-1`, không phải một khoảng thời gian để hiện). `list_schema` của Redis giờ gọi thêm `TTL` cho mỗi key cạnh `TYPE` sẵn có (N+2 round trip thay vì N+1, cùng đánh đổi đã chấp nhận cho `TYPE`); `BrowseSidebarComponent` hiện `ttl:3421s` cạnh type khi có. Dòng echo lệnh: `QueryDriver` có thêm `browse_command(entry) -> Option<String>` (mặc định `None`, cùng pattern với `browse_entry`) — trả về đúng chuỗi lệnh `browse_entry` sẽ chạy mà **không** chạy nó, để sidebar echo trước khi round-trip xong. `QueryScreenComponent.last_browse_command` set trong `open_selected_key()` ngay khi Enter được bấm, vẽ thành một dòng `<target>> <command>` dưới bảng kết quả trong layout Browse mode (carve 1 dòng cuối từ cột kết quả, chỉ khi có lệnh để hiện — không tốn dòng ở chế độ Console hay khi chưa browse gì). Field `SchemaInfo.ttl` mới kéo theo sửa mọi nơi dựng `SchemaInfo` bằng struct literal thay vì `::new()` (Cassandra, Elasticsearch, Mongo, SQLite, và các literal test) — Postgres không đụng vì đã dùng `SchemaInfo::new()`. Test: unit cho `browse_command` (đúng lệnh / `None` với type không hỗ trợ), render test cho TTL hiện/không hiện trong sidebar, integration test trong `query_screen.rs` xác nhận Enter set đúng `last_browse_command` và dòng đó xuất hiện trong frame vẽ ra.

Hai điểm mockup có nhưng **đã xét và cố tình không làm giống hệt** (không phải gap mới, khỏi nêu lại): autocomplete không hiện kiểu cột trong popup gợi ý (mục 5 cũ, xong 2026-08-14 — chỉ tên, không kiểu); thanh trạng thái auto-commit không hiện liên tục "● auto-commit: ON" như mockup mà chỉ lên tiếng khi có transaction mở (mục 3 ở trên, "thu hẹp phạm vi... giống hệt cách `psql` tự báo").

## Vấn đề đã biết (gác lại, không chặn)

Từ các review nhiệm vụ và review toàn nhánh cuối cùng của migration Component Architecture (ghi lại ở đây để không bị mất):

- ~~**Postgres connect bị treo 30s không có phản hồi khi host không reachable.**~~ Đã fix. `PostgresDriver::connect()` từng dùng `PgPool::connect()`, kế thừa `acquire_timeout` mặc định 30s của sqlx — khi bị từ chối kết nối, TUI trông như đứng hoàn toàn (không lỗi, không dấu hiệu gì đang chạy) suốt 30 giây trước khi có `PoolTimedOut`. Giờ dùng `PgPoolOptions::new().acquire_timeout(Duration::from_secs(5)).connect(...)`. Test hồi quy: `drivers::postgres::tests::connect_fails_quickly_against_an_unreachable_host` trong `drivers/postgres/mod.rs` (nay `crates/tradar-app/src/drivers/postgres/mod.rs`), không cần Docker (connect tới port 1, luôn bị từ chối) và giới hạn thời gian chờ bằng `tokio::time::timeout`.
- ~~**Race điều kiện connect-vs-connect.**~~ Đã fix. `ConnectionPickerComponent` giờ giữ một bộ đếm `connect_epoch: u64`, tăng lên và gắn vào `Action::ConnectRequested` mỗi lần thử connect; `spawn_connect` trong `main.rs` truyền epoch đó xuyên suốt tới `Connected`/`ConnectFailed` gửi ngược lại. `RootComponent::update` bỏ qua bất kỳ `Connected`/`ConnectFailed` nào có epoch không khớp `connection_picker.connect_epoch`, nên một reply từ một lần connect đã bị thay thế không còn âm thầm chuyển UI về nữa. Cùng pattern với cơ chế query-epoch đã có sẵn trong `QueryScreenComponent`. Được cover bởi `components::tests::a_stale_connected_from_a_superseded_connect_attempt_is_ignored` và `...a_stale_connect_failed_from_a_superseded_connect_attempt_is_ignored` trong `components/mod.rs` (nay `crates/tradar-app/src/components/mod.rs`).
- ~~**`ConnectionPickerComponent.last_error` không bao giờ được clear khi retry thành công.**~~ Đã fix (2026-08-12, sub-project 5). `Enter` giờ clear `last_error` ngay khi một lần connect mới bắt đầu, trước khi phát `Action::OpenRequested`. Test: `a_new_connect_attempt_clears_a_stale_error_from_a_previous_one` trong `connection_picker.rs`.
- ~~Chưa có test nào kiểm tra `Ctrl+Y` khi một panel khác đang giữ keyboard focus.~~ Đã có (`ctrl_y_exports_curl_even_while_the_results_pane_has_focus` trong `query_screen.rs`; trước 2026-08-14 tên nó nhắc schema sidebar, panel đó nay đã thành navigator ở app shell).
- ~~Không nhất quán về style `Default`/`new()` giữa `ResultsComponent`/`QueryEditorComponent`/`SchemaSidebarComponent` (component thứ ba nay đã bị xoá — xem mục 6).~~ Đã fix (2026-08-12, sub-project 5). `ResultsComponent` đổi sang cùng pattern với hai cái kia: hand-write `Default` delegate sang `new()`, bỏ `#[derive(Default)]` (từng duplicate y hệt logic của `new()`).
- ~~`main.rs`'s event loop vẽ lại vô điều kiện mỗi vòng lặp (tới 20 lần/giây).~~ Đã fix (2026-08-12, sub-project 5). `Component::tick`/`Session::tick` đổi sang trả `bool` ("có thay đổi cần vẽ lại không", mặc định `false`); `main.rs` theo dõi một cờ `dirty` qua key input/action/connect outcome/tick, chỉ gọi `terminal.draw` khi cờ đó bật.
- ~~**Click chuột vào ô kết quả bị lag, càng xa (khác cột lẫn khác dòng) càng chậm — user báo (2026-08-15).**~~ Đã fix. Nguyên nhân: `EnableMouseCapture` của crossterm bật chế độ báo cả di chuyển chuột (không chỉ click/scroll) trên nhiều terminal — mỗi lần chuột lướt qua màn hình giữa vị trí cũ và ô vừa bấm sinh ra một loạt sự kiện `Moved`/`Drag`. Event loop trong `main.rs` xử lý **đúng một** sự kiện mỗi vòng lặp (`event::poll` + `event::read`), và trước đây set `dirty = true` cho **mọi** `Event::Mouse` bất kể loại — kể cả loại `handle_mouse_event` không làm gì với nó (nhánh `_ => {}` của nó). Kết quả: bấm vào một ô càng xa vị trí chuột trước đó thì càng nhiều sự kiện di chuyển bị kẹt trong hàng đợi, mỗi cái kéo theo một lần `terminal.draw` đầy đủ trước khi sự kiện click thật sự được xử lý — độ trễ tỉ lệ thuận với khoảng cách di chuyển, đúng như user mô tả (cùng cột thì nhanh vì quãng đường ngắn, khác cột khác dòng thì chậm vì quãng đường dài hơn). Fix: chỉ set `dirty`/gọi `handle_mouse_event` cho đúng 3 loại sự kiện app thật sự phản ứng (`Down(Left)`, `ScrollDown`, `ScrollUp`), lọc bỏ mọi loại khác ngay tại vòng lặp — không đụng gì tới `handle_mouse_event` của từng component (logic click/scroll vẫn y nguyên). Không có unit test cho `main.rs` (event loop thô, không phải `Component`) nên verify hoàn toàn bằng tay: gửi thẳng escape sequence SGR mouse click (`\x1b[<0;COL;ROWM`) qua tmux vào connection picker và xác nhận dòng được click vẫn lên đúng màu chọn (`48;5;238`) sau khi lọc.
- ~~**Picker không có phản hồi gì khi đang connect, kể cả khi thất bại — user báo (2026-08-15).**~~ Đã fix. `Action::OpenFailed` vốn đã set `last_error` và hiện box lỗi đúng (đã có test từ trước) — cái thiếu thật sự là **lúc đang chờ**: bấm `Enter` xong màn hình picker trông y hệt lúc chưa bấm gì suốt tới 5s (giới hạn `CONNECT_TIMEOUT`), như thể app treo. Thêm `ConnectionPickerComponent.connecting: Option<String>` (tên connection đang thử) — set trong `open_selected()` cùng lúc với clear `last_error`, đổi tiêu đề panel thành `Connections — connecting to '<name>'…`. Clear ở hai chỗ: `RootComponent::update` (cả nhánh `Opened` lẫn `OpenFailed`, khớp epoch) và `ConnectionPickerComponent::update` của chính nó (cùng chỗ set `last_error`) — cả hai đường cùng phải clear vì `RootComponent::update` set field trực tiếp thay vì gọi qua `connection_picker.update()`, hai đường logic riêng biệt đã tồn tại từ trước. Verify tay qua tmux: connect vào một Postgres không reachable (`10.255.255.1`), thấy tiêu đề đổi thành "connecting to 'unreachable-pg'…" ngay lập tức, đợi hết 5s thấy tiêu đề trở lại bình thường và box lỗi "pool timed out..." hiện ra.
- `event::poll(Duration::from_millis(50))` trong `main.rs` là một lời gọi đồng bộ có thể block worker thread tokio của nó tới 50ms mỗi tick rảnh. Có giới hạn và không ảnh hưởng tính đúng đắn trên runtime multi-threaded mặc định, nhưng `crossterm::event::EventStream` + `tokio::select!` sẽ là lựa chọn không-blocking nếu điều này từng trở thành vấn đề thật. Vẫn để nguyên — ưu tiên thấp, không liên quan tới việc redraw đã fix ở trên (đây là block trong lúc *chờ* input, không phải vẽ lại thừa).
- ~~**`Ctrl+B` (toggle navigator) "lúc ăn lúc không", phải bấm nhanh mới nhận — user báo (2026-08-15).**~~ Điều tra xong, **không phải bug của `tradar`**. Gốc rễ: `Ctrl+B` là phím prefix mặc định của `tmux`, khi user chạy `tradar` bên trong một session tmux thì tmux tự nuốt phím đó trước để chờ lệnh kế tiếp — hành vi thất thường tuỳ timing hoàn toàn nằm ở tầng tmux, `resolve_in` của `tradar` khi nhận được key event vẫn resolve `ctrl-b` → `ToggleNavigator` đúng 100% mọi lần khi test bằng cách gửi thẳng key event (bỏ qua tầng tmux). User chọn remap riêng (`~/.config/tradar/config.toml`, mục `[keymap.global]`, `toggle-navigator = "ctrl-g"` hoặc phím tuỳ chọn) thay vì đổi default toàn project — **quyết định đã chốt, đừng tự đổi default trong `keymap.rs` nếu không có yêu cầu mới**. Ghi lại ở đây phòng khi user khác báo y hệt: default vẫn là `ctrl-b`, biết trước là đụng tmux nhưng chưa đủ lý do đổi cho tất cả (user không dùng tmux, hoặc đã đổi prefix tmux, thì `ctrl-b` không có vấn đề gì).
- ~~**Redraw/recompute thừa trong lúc query đang chạy** — user yêu cầu rà hiệu năng tổng thể (2026-08-16), sau khi spinner (mục "Không có đồng hồ đếm giờ" ở trên) khiến `dirty` được set ~20 lần/giây suốt thời gian query chạy.~~ Đã fix, sau một đợt audit 5 hướng song song (redraw/dirty-flag, autocomplete, cây navigator, syntax highlight, chi phí `draw()` tổng thể):
  - **`QueryEditorComponent`**: `line_colors()` (parse tree-sitter toàn buffer) và `foldable_ranges()` (tách câu SQL toàn buffer) chạy lại **vô điều kiện mỗi `draw()`** — kể cả khi buffer không đổi. Thêm cache `Memoized<T> = RefCell<Option<(Vec<Vec<char>>, T)>>` (field `fold_cache`/`highlight_cache`), so khớp bằng chính nội dung buffer chứ không phải cờ dirty tự set tay — nên không thể lỗi thời dù đường nào làm buffer thay đổi. Dùng `RefCell` để hai hàm này vẫn giữ nguyên `&self`, không lan chữ ký `&mut self` ra khắp các hàm gọi (`fold_containing`, `hidden_rows`, `cursor_screen_position`...). Test: `foldable_ranges_reflects_an_edit_after_the_cache_is_warm`, `line_colors_reflects_an_edit_after_the_cache_is_warm` trong `query_editor.rs`.
  - **`QueryScreenComponent::outline()`**: build lại + clone toàn bộ schema đã flatten (tên/kiểu mọi bảng/cột) mỗi lần navigator gọi — mỗi phím bấm, mỗi frame, cho **mọi tab đang mở** dù subtree đó có bung hay không. Vì `engine.schema()` bất biến sau khi connect, giờ tính đúng một lần trong `new()` (hàm rời `flatten_outline`), lưu vào field `outline`, trait method chỉ `.clone()` field đó ra.
  - **`CompletionSource::matches()`**: lowercase lại text của mọi candidate mỗi lần gõ phím, rồi lowercase lại **lần nữa** trong chính sort key của nó. `Candidate` giờ có field `text_lower` tính sẵn lúc build (`Candidate::new`); dedupe cột trùng tên khi build đổi từ scan `O(n²)` (`candidates.iter().any(...)`) sang `HashSet`.
  - **`QueryEngine::tick()`**: điều kiện "redraw khi query đang chạy" (thêm cùng đợt với spinner) trước đó set `changed = true` mỗi lần gọi — tới ~20 lần/giây theo chu kỳ poll 50ms của `main.rs`, dù glyph spinner chỉ thật sự đổi mỗi 80ms. Giờ so khớp frame spinner (`elapsed / SPINNER_FRAME_MS`, hằng số dùng chung với `results.rs` để hai bên không lệch nhau) với frame đã báo lần cuối, chỉ set `changed` khi frame đó thật sự tăng — còn ~12.5 lần/giây, đúng bằng nhịp mắt còn nhìn thấy khác biệt.
  - **Đã audit và xác nhận đã tối ưu, không đụng**: `main.rs`'s event loop + toàn bộ `Component::tick()`/`Session::tick()` khác (RootComponent, Kafka/RabbitMQ screen) chỉ báo `changed` đúng lúc có thay đổi thật; `NavigatorComponent::visible_rows()` đã là O(n) (fix ở commit trước trong đợt rà mockup); `ResultsComponent::draw()` khi có spinner **return sớm** trước khi chạm tới `column_widths()`/`filter_table_rows()` nên các quét toàn bảng kết quả không hề chạy lại trong lúc query chạy.
- ~~**Mở một connection chưa kết nối từ navigator trong khi tab hiện tại đang bận sẽ tự tạo tab mới rồi nhảy sang luôn — user báo thấy không hợp lý (2026-08-16).**~~ Đã fix. `open_connection()` (`RootComponent`, trong `components/mod.rs`) trước đây: nếu tab đang active đã `Active` (đang kết nối connection khác), gọi `new_tab()` — hàm này vừa tạo tab vừa set `active_tab` sang tab mới luôn, nên bấm mở một connection chưa kết nối trong navigator sẽ đột ngột kéo người dùng ra khỏi tab đang làm việc, không hỏi han gì. Việc này đi ngược tinh thần của chính navigator: `NavOutcome::Insert`/`Snippet` (chèn tên vào editor) và `Focus` (nhảy tới connection đã mở sẵn) đều là hành động **chủ đích** nên nhảy tab là đúng, nhưng `Open` trên một connection **chưa kết nối** chỉ là "with tay xem/mở từ xa" — kiến trúc đã chốt trước đó bắt buộc "bấm mở chính là connect" (không có state xem trước mà chưa connect), nhưng connect không nhất thiết phải kéo theo đổi focus. Sau khi hỏi lại và chốt hướng qua `AskUserQuestion`: **vẫn tạo tab mới + connect ngầm ở đó, nhưng không đổi `active_tab`** — giữ đúng tinh thần "reach across mà không rời chỗ đang làm", giống hệt cách một tab nền mở trong trình duyệt (không cắt ngang, nhưng vẫn xuất hiện trên tab bar và tự nối khi xong; `Ctrl+Right` tới được như mọi tab khác). Navigator cũng **không đóng/mất focus** sau `Open` (vốn đã vậy từ trước, không đổi) — để with tiếp connection khác được luôn mà không cần mở lại `Ctrl+B`. Implementation: `previously_active` chụp lại `active_tab` *trước* khi `new_tab()` chạy, khôi phục lại ngay sau khi build xong `Action::OpenRequested` — action đó tự mang theo `tab` (index tab nền, chụp lúc `new_tab()` vừa chạy) nên pipeline connect ở `main.rs` không phụ thuộc gì vào `active_tab` đang là bao nhiêu, an toàn tuyệt đối để tách hai việc "connect" và "đổi focus" ra khỏi nhau. **Chưa làm** (biết trước, chưa đủ lý do làm ngay): không có badge/thông báo khi tab nền connect xong hoặc connect lỗi — lỗi ở tab nền chỉ thấy khi tự chuyển sang tab đó xem, giống hệt cách một tab nền lỗi trong trình duyệt cũng không luôn báo ngay. Test: `opening_an_unconnected_connection_from_the_navigator_stays_on_the_current_tab` trong `components/mod.rs` (xác nhận `active_tab` không đổi + navigator vẫn focus sau khi mở); test cũ `..._requests_a_connect` giữ nguyên không sửa (vẫn đúng: action nhắm đúng tab nền, `tabs.len()` tăng lên 2).
