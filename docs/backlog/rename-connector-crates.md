# Đổi tên + di chuyển connector crate: `crates/connectors/tradar-*` → `crates/tradar-connector-*` (user yêu cầu 2026-08-16) — xong

User hỏi trước liệu có tách được mỗi connector thành binary độc lập trong khi vẫn chung codebase không — trả lời khả thi (tách phần lõi `tradar-app` thành `lib.rs` + nhiều `src/bin/*.rs`, dùng Cargo feature per-connector để mỗi binary chỉ link đúng driver cần) nhưng đó là việc lớn, chưa làm ngay. User chốt: **giữ nguyên kiến trúc hiện tại** (một binary `tradar` duy nhất, chưa tách multi-binary), chỉ dọn lại cấu trúc thư mục — bỏ hẳn thư mục lồng `crates/connectors/`, chuyển 9 connector crate ra sống trực tiếp dưới `crates/`, đổi prefix từ `tradar-<tên>` thành `tradar-connector-<tên>` (v.d. `tradar-postgres` → `tradar-connector-postgres`) để tên crate tự nói rõ vai trò, đồng thời cũng là bước dọn đường nếu multi-binary được làm sau này (feature flag đặt tên theo connector sẽ khớp tự nhiên với tên crate).

Việc thuần cơ học, không đổi trait/API/behavior nào:
- `git mv` từng thư mục (`tradar-http` mới thêm trong session này chưa commit nên dùng `mv` thường + `git add`), xoá thư mục `crates/connectors/` rỗng sau khi chuyển hết.
- Mỗi `Cargo.toml` connector: đổi `name = "tradar-<tên>"` → `name = "tradar-connector-<tên>"`, và rút ngắn path dependency `../../tradar-core` → `../tradar-core` (ít hơn một cấp thư mục vì không còn lồng qua `connectors/`) — tương tự cho `tradar-connector-api`/`tradar-query-workbench`.
- `Cargo.toml` gốc (workspace members) và `crates/tradar-app/Cargo.toml` (path dependency + tên package) cập nhật theo tên/đường dẫn mới.
- `crates/tradar-app/src/main.rs`'s `registry()`: `tradar_postgres::connector()` → `tradar_connector_postgres::connector()` (Rust tự đổi `-` thành `_` trong path, nên tên crate với prefix dài hơn kéo theo path gọi dài hơn tương ứng) cho cả 9 connector.
- `Makefile`: mọi `-p tradar-<tên>` và `--exclude tradar-<tên>` đổi theo tên mới (`test-unit`, `test-postgres`, ..., `test-http`). **Cố tình không đổi** `DOCKER_SERVICES` (danh sách service của `docker-compose.yml`) — đó là tên service Docker, không phải tên crate, hai khái niệm khác nhau dù trùng chữ ở phần lớn trường hợp.
- Doc comment trong code tham chiếu tên crate khác bằng chữ (không phải qua Cargo, nên compiler không tự bắt lỗi được) cũng dò và sửa: `tradar-connector-kafka`'s module doc ("like `tradar-rabbitmq`" → "like `tradar-connector-rabbitmq`"), `tradar-connector-mongo`'s doc cho `flatten_document` (nhắc `tradar-elasticsearch`), `tradar-core::storage`'s doc cho `SavedHttpRequest`/`HttpRequests` (nhắc `tradar-http`).
- `CLAUDE.md`/`README.md`/`docs/architecture.md`: cập nhật số crate (12 → 13, tính cả HTTP mới thêm phiên trước), đường dẫn/tên crate trong "Triển khai hiện tại" + "Bố cục workspace" (cả cây thư mục ASCII lẫn văn xuôi), và mọi chỗ liệt kê dependency/registry nhắc tên connector cụ thể. **Cố tình không sửa** `docs/backlog.md`'s các mục lịch sử phía trên (mục "Bước 4", mục Cassandra/Kafka/RabbitMQ, ...) — đó là bản ghi quyết định tại thời điểm viết, tên/đường dẫn cũ vẫn đúng với "lúc đó", sửa lại toàn bộ văn xuôi lịch sử cho một việc đổi tên thuần tuý tốn công không tương xứng; cũng không sửa `docs/next-run.md` (cùng lý do — log lịch sử của bước 1 migration). Tương tự, `docs/architecture.md` vẫn giữ nguyên câu chuyện lịch sử ("Bố cục workspace" mô tả state tại 2026-08-10 dùng `(tương lai) tradar-kafka/...`), chỉ chêm một câu **Cập nhật 2026-08-16** ngay sau để người đọc biết cây thư mục vẽ trong đó không còn khớp thực tế nữa, phải xem "Triển khai hiện tại" ở đầu file để có cây đúng — giữ tinh thần "ghi lại lịch sử, không viết lại lịch sử" nhất quán với cách backlog.md vẫn làm.
- Verify: `cargo build --workspace`, `cargo fmt --check`, `cargo clippy --all-targets --workspace -- -D warnings`, `make test-unit` (11 test binary, tất cả `ok`) đều sạch sau khi đổi.

